{
    "version": 1,
    "recordedOn": "2026-08-24",
    "note": "Ceilings for `bun run check:ui` (issue #2580). Recorded from a real run on this machine \u2014 see the PR. The floors this lane is FOR are cardsOcc 0, ctrlsStranded 0 and axeSerious/axeCritical 0. AS OF ISSUE #2593 EVERY WALKED SURFACE HOLDS axeSerious 0 / axeCritical 0 \u2014 those two are no longer debt lines anywhere, so a nonzero reading is a regression, not a carried defect. The one surface that could not hold it as written is `/admin/design-system`, whose job includes SHOWING what a failing token looks like: its three deliberate specimens carry `data-axe-exempt=\"<why>\"`, the lane excludes those subtrees and prints `exempt<n>` on the surface's line of every run, and `scripts/__tests__/axe-exemption-scope.test.ts` fails if the attribute appears anywhere but that page. An exemption is a named node in a reviewable diff; a nonzero budget row is a number nobody can attribute. Where a surface breaks one of the REMAINING floors today; where a surface breaks one of those TODAY the entry keeps the measured value and carries a `knownDebt` note naming the defect, printed under every run. A slice that fixes a surface lowers its ceilings in the same PR. A surface nobody can walk yet is `{\"status\":\"unwalked\",\"reason\":\u2026}` \u2014 listed in the output and in the coverage line, never silently green. Issue #2582 (PR #2619) moved the probe's `occ`/`reachable` boundary, and it moved it in BOTH directions. A control is `occ` when the centre of its VISIBLE INTERSECTION with (viewport n its own SCROLL PORT) hit-tests to something else, and `reachable` when that intersection is EMPTY but a gesture can bring it back \u2014 see `probe.js`'s `scrollPort` / `probe`. The old rule tested the element's raw centre against the bare viewport. LOOSER than the old rule: a control lying wholly outside its port's box (the deck-builder's off-strip `Delete column MV n` buttons) is no longer `occ`, and neither is one straddling a port's clip edge with a band below it (the lobby's `Your Events (all)` under the new bottom nav) \u2014 `<main>` paints nothing there, so nothing can be occluding it. STRICTER than the old rule: a control PARTLY clipped by its port is now hit-tested over the strip it still paints, where a raw centre would have been thrown away as out-of-window (`design-system-dialog`'s section-08 Cancel/Confirm specimens), and a `position: fixed` box gets no port at all rather than borrowing an ancestor scroller's rectangle. Rows re-recorded against the corrected probe carry the measurement and its enumeration in their own note. `deck-builder` was re-recorded whole on 2026-08-20 (issue #2585 fixup, PR #2650): the filter controls leaving the header band for a sheet/popover freed real height at every viewport, and a follow-up fixup (search bar `min-w-0 overflow-hidden`, Filters button icon-only under `short-viewport:`) closed a same-day regression at `844x390x3` where the new Filters button wrapped the header onto two full-height rows and pushed the Maindeck's Delete-column row under `SaveDeckBar` (`ctrlsOcc` 2 \u2192 5 mid-fix \u2192 1 final). Rows not re-recorded still measure below their number and are deliberately left alone: those readings vary with the deployment's own deck/pool data, and the notes on them describe debt the surface's own slice owns. Issue #2658 adds `small`: count of visible in-band interactive controls under 44px on their smaller dimension (`probe.js`'s `smallN`). This key is DELIBERATELY POINTER-BLIND \u2014 the probe applies the same 44px test at every viewport, but `--control-h` resolves to 32px under `pointer: fine` ON PURPOSE (`src/index.css:942,946-948`, citing WCAG 2.5.8, a TOUCH-target rule). So the `1440x900x2` (desktop, pointer: fine) row of every surface reads a nonzero `small` that is the intended 32px control height, not debt; the four `\u2026,mobile,touch\u2026` rows (390x844x3, 844x390x3, 820x1180x2, 1180x820x2) read real sub-44px tap-target debt. Every nonzero `small` ceiling below carries a `knownDebt` note saying explicitly which of the two it is \u2014 a reader must never \"fix\" a desktop reading by shrinking a touch target, or dismiss a touch reading as \"just the desktop thing\". This ticket only RECORDS the touch-viewport debt as a ceiling so it cannot deepen; #2659 owns lowering it.",
    "surfaces": {
        "auth-sign-in": {
            "label": "Sign in (signed out, /)",
            "status": "budgeted",
            "viewports": {
                "1440x900x2": {
                    "cardsZero": 0,
                    "cardsOcc": 0,
                    "cardsStranded": 0,
                    "ctrlsZero": 0,
                    "ctrlsOcc": 0,
                    "ctrlsStranded": 0,
                    "starved": 0,
                    "small": 2,
                    "axeSerious": 0,
                    "axeCritical": 0,
                    "knownDebt": "`small` here is the shared `.input-field` recipe, not this screen: `src/index.css:498-499` is `px-4 py-2 text-sm`, ~36px tall at every viewport and every pointer type \u2014 the deliberate deferral recorded in that file's own comment (issue #2581), which owns raising it app-wide. It reads on the DESKTOP row too, unlike the `--control-h-fine` 32px case the file note above describes, because the recipe carries no pointer branch at all. The auth screen's OTHER controls all clear 44px: the submit is `size=\"lg\"` and the footer text links carry `min-h-11` (added with the reset flow, so a new tap target did not land under the floor). Do NOT lower this ceiling by shrinking the panel."
                },
                "390x844x3": {
                    "cardsZero": 0,
                    "cardsOcc": 0,
                    "cardsStranded": 0,
                    "ctrlsZero": 0,
                    "ctrlsOcc": 0,
                    "ctrlsStranded": 0,
                    "starved": 0,
                    "small": 2,
                    "axeSerious": 0,
                    "axeCritical": 0,
                    "knownDebt": "`small` here is the shared `.input-field` recipe, not this screen: `src/index.css:498-499` is `px-4 py-2 text-sm`, ~36px tall at every viewport and every pointer type \u2014 the deliberate deferral recorded in that file's own comment (issue #2581), which owns raising it app-wide. It reads on the DESKTOP row too, unlike the `--control-h-fine` 32px case the file note above describes, because the recipe carries no pointer branch at all. The auth screen's OTHER controls all clear 44px: the submit is `size=\"lg\"` and the footer text links carry `min-h-11` (added with the reset flow, so a new tap target did not land under the floor). Do NOT lower this ceiling by shrinking the panel."
                },
                "844x390x3": {
                    "cardsZero": 0,
                    "cardsOcc": 0,
                    "cardsStranded": 0,
                    "ctrlsZero": 0,
                    "ctrlsOcc": 0,
                    "ctrlsStranded": 0,
                    "starved": 1,
                    "small": 2,
                    "axeSerious": 0,
                    "axeCritical": 0,
                    "knownDebt": "`small` here is the shared `.input-field` recipe, not this screen: `src/index.css:498-499` is `px-4 py-2 text-sm`, ~36px tall at every viewport and every pointer type \u2014 the deliberate deferral recorded in that file's own comment (issue #2581), which owns raising it app-wide. It reads on the DESKTOP row too, unlike the `--control-h-fine` 32px case the file note above describes, because the recipe carries no pointer branch at all. The auth screen's OTHER controls all clear 44px: the submit is `size=\"lg\"` and the footer text links carry `min-h-11` (added with the reset flow, so a new tap target did not land under the floor). Do NOT lower this ceiling by shrinking the panel. `starved 1` is the screen's own scroll port at 844x390: the sign-in panel is taller than a 390px-tall landscape phone, so the `h-svh` container scrolls it. This is the GOOD end of a trade the lane surfaced \u2014 the container used to be `min-h-svh overflow-hidden` with the panel centred on the cross axis, which read `ctrlsStranded 1`: the footer links overflowed above and below the clip with no gesture able to reach them. A container that scrolls its own overflow is not the deck-builder pathology this key was added for (a 24-66px window around a 101px card tile); lowering it means making the panel shorter at landscape phone, which is a separate design call."
                },
                "820x1180x2": {
                    "cardsZero": 0,
                    "cardsOcc": 0,
                    "cardsStranded": 0,
                    "ctrlsZero": 0,
                    "ctrlsOcc": 0,
                    "ctrlsStranded": 0,
                    "starved": 0,
                    "small": 2,
                    "axeSerious": 0,
                    "axeCritical": 0,
                    "knownDebt": "`small` here is the shared `.input-field` recipe, not this screen: `src/index.css:498-499` is `px-4 py-2 text-sm`, ~36px tall at every viewport and every pointer type \u2014 the deliberate deferral recorded in that file's own comment (issue #2581), which owns raising it app-wide. It reads on the DESKTOP row too, unlike the `--control-h-fine` 32px case the file note above describes, because the recipe carries no pointer branch at all. The auth screen's OTHER controls all clear 44px: the submit is `size=\"lg\"` and the footer text links carry `min-h-11` (added with the reset flow, so a new tap target did not land under the floor). Do NOT lower this ceiling by shrinking the panel."
                },
                "1180x820x2": {
                    "cardsZero": 0,
                    "cardsOcc": 0,
                    "cardsStranded": 0,
                    "ctrlsZero": 0,
                    "ctrlsOcc": 0,
                    "ctrlsStranded": 0,
                    "starved": 0,
                    "small": 2,
                    "axeSerious": 0,
                    "axeCritical": 0,
                    "knownDebt": "`small` here is the shared `.input-field` recipe, not this screen: `src/index.css:498-499` is `px-4 py-2 text-sm`, ~36px tall at every viewport and every pointer type \u2014 the deliberate deferral recorded in that file's own comment (issue #2581), which owns raising it app-wide. It reads on the DESKTOP row too, unlike the `--control-h-fine` 32px case the file note above describes, because the recipe carries no pointer branch at all. The auth screen's OTHER controls all clear 44px: the submit is `size=\"lg\"` and the footer text links carry `min-h-11` (added with the reset flow, so a new tap target did not land under the floor). Do NOT lower this ceiling by shrinking the panel."
                }
            }
        },
        "auth-forgot-password": {
            "label": "Password reset, step 1 (signed out, / \u2192 Forgot password?)",
            "status": "budgeted",
            "viewports": {
                "1440x900x2": {
                    "cardsZero": 0,
                    "cardsOcc": 0,
                    "cardsStranded": 0,
                    "ctrlsZero": 0,
                    "ctrlsOcc": 0,
                    "ctrlsStranded": 0,
                    "starved": 0,
                    "small": 1,
                    "axeSerious": 0,
                    "axeCritical": 0,
                    "knownDebt": "`small` here is the shared `.input-field` recipe, not this screen: `src/index.css:498-499` is `px-4 py-2 text-sm`, ~36px tall at every viewport and every pointer type \u2014 the deliberate deferral recorded in that file's own comment (issue #2581), which owns raising it app-wide. It reads on the DESKTOP row too, unlike the `--control-h-fine` 32px case the file note above describes, because the recipe carries no pointer branch at all. The auth screen's OTHER controls all clear 44px: the submit is `size=\"lg\"` and the footer text links carry `min-h-11` (added with the reset flow, so a new tap target did not land under the floor). Do NOT lower this ceiling by shrinking the panel."
                },
                "390x844x3": {
                    "cardsZero": 0,
                    "cardsOcc": 0,
                    "cardsStranded": 0,
                    "ctrlsZero": 0,
                    "ctrlsOcc": 0,
                    "ctrlsStranded": 0,
                    "starved": 0,
                    "small": 1,
                    "axeSerious": 0,
                    "axeCritical": 0,
                    "knownDebt": "`small` here is the shared `.input-field` recipe, not this screen: `src/index.css:498-499` is `px-4 py-2 text-sm`, ~36px tall at every viewport and every pointer type \u2014 the deliberate deferral recorded in that file's own comment (issue #2581), which owns raising it app-wide. It reads on the DESKTOP row too, unlike the `--control-h-fine` 32px case the file note above describes, because the recipe carries no pointer branch at all. The auth screen's OTHER controls all clear 44px: the submit is `size=\"lg\"` and the footer text links carry `min-h-11` (added with the reset flow, so a new tap target did not land under the floor). Do NOT lower this ceiling by shrinking the panel."
                },
                "844x390x3": {
                    "cardsZero": 0,
                    "cardsOcc": 0,
                    "cardsStranded": 0,
                    "ctrlsZero": 0,
                    "ctrlsOcc": 0,
                    "ctrlsStranded": 0,
                    "starved": 0,
                    "small": 1,
                    "axeSerious": 0,
                    "axeCritical": 0,
                    "knownDebt": "`small` here is the shared `.input-field` recipe, not this screen: `src/index.css:498-499` is `px-4 py-2 text-sm`, ~36px tall at every viewport and every pointer type \u2014 the deliberate deferral recorded in that file's own comment (issue #2581), which owns raising it app-wide. It reads on the DESKTOP row too, unlike the `--control-h-fine` 32px case the file note above describes, because the recipe carries no pointer branch at all. The auth screen's OTHER controls all clear 44px: the submit is `size=\"lg\"` and the footer text links carry `min-h-11` (added with the reset flow, so a new tap target did not land under the floor). Do NOT lower this ceiling by shrinking the panel."
                },
                "820x1180x2": {
                    "cardsZero": 0,
                    "cardsOcc": 0,
                    "cardsStranded": 0,
                    "ctrlsZero": 0,
                    "ctrlsOcc": 0,
                    "ctrlsStranded": 0,
                    "starved": 0,
                    "small": 1,
                    "axeSerious": 0,
                    "axeCritical": 0,
                    "knownDebt": "`small` here is the shared `.input-field` recipe, not this screen: `src/index.css:498-499` is `px-4 py-2 text-sm`, ~36px tall at every viewport and every pointer type \u2014 the deliberate deferral recorded in that file's own comment (issue #2581), which owns raising it app-wide. It reads on the DESKTOP row too, unlike the `--control-h-fine` 32px case the file note above describes, because the recipe carries no pointer branch at all. The auth screen's OTHER controls all clear 44px: the submit is `size=\"lg\"` and the footer text links carry `min-h-11` (added with the reset flow, so a new tap target did not land under the floor). Do NOT lower this ceiling by shrinking the panel."
                },
                "1180x820x2": {
                    "cardsZero": 0,
                    "cardsOcc": 0,
                    "cardsStranded": 0,
                    "ctrlsZero": 0,
                    "ctrlsOcc": 0,
                    "ctrlsStranded": 0,
                    "starved": 0,
                    "small": 1,
                    "axeSerious": 0,
                    "axeCritical": 0,
                    "knownDebt": "`small` here is the shared `.input-field` recipe, not this screen: `src/index.css:498-499` is `px-4 py-2 text-sm`, ~36px tall at every viewport and every pointer type \u2014 the deliberate deferral recorded in that file's own comment (issue #2581), which owns raising it app-wide. It reads on the DESKTOP row too, unlike the `--control-h-fine` 32px case the file note above describes, because the recipe carries no pointer branch at all. The auth screen's OTHER controls all clear 44px: the submit is `size=\"lg\"` and the footer text links carry `min-h-11` (added with the reset flow, so a new tap target did not land under the floor). Do NOT lower this ceiling by shrinking the panel."
                }
            }
        },
        "lobby": {
            "label": "Lobby (/)",
            "status": "budgeted",
            "viewports": {
                "1440x900x2": {
                    "cardsZero": 0,
                    "cardsOcc": 1,
                    "cardsStranded": 0,
                    "ctrlsZero": 0,
                    "ctrlsOcc": 0,
                    "ctrlsStranded": 0,
                    "starved": 2,
                    "small": 24,
                    "axeSerious": 0,
                    "axeCritical": 0,
                    "knownDebt": "2 starved scroll containers \u2014 hard floors are cardsOcc 0 / axeSerious 0. The `nested-interactive` serious that used to sit here is GONE (issue #2593): the deck row was `<div role=\"button\" tabIndex={0}>` wrapped around the Select button, and it is now an inert container with one absolutely-positioned overlay button carrying the row-wide gesture (`deck-list-item.tsx`). `cardsOcc 1` is NOT a deck tile: it is the same probe artifact as the `design-system-dialog` row, and it is NONDETERMINISTIC. `LobbyBackground` renders `AmbientPageGround` (`src/components/lobby/lobby-background.tsx:13`), whose decorative full-bleed `<img data-ambient-art aria-hidden>` draws its `src` at random per mount from `AMBIENT_BG_IMAGES` (8 local `/img/lobby-bg/*.webp` + 21 `cards.scryfall.io` art crops); only a Scryfall draw matches `probe.js:137-141`'s `img[src*=\"scryfall\"]`, and it then scores occluded behind the opaque signal panels. Measured on one unchanged tree: `n39/occ1 -> n38/occ0` between consecutive runs; pinning the draw to a local frame gives `n38/occ0` at every viewport. The ceiling is held at 1 so a Scryfall draw does not red the lane. Drafted for #2580's lane owner in `docs/findings/2581-ui-gate-card-probe-counts-ambient-backdrop.md`. `small24` at 1440x900x2 (pointer: fine) is the deliberate 32px `--control-h-fine` branch (`src/index.css:942,946-948`) showing up in a POINTER-BLIND measurement, not a defect \u2014 WCAG 2.5.8 (cited in that CSS comment) is a TOUCH-target rule and does not bind a mouse pointer. Do NOT lower this ceiling by shrinking desktop controls; #2659 (the sibling fix issue) does not touch this row."
                },
                "390x844x3": {
                    "cardsZero": 0,
                    "cardsOcc": 1,
                    "cardsStranded": 0,
                    "ctrlsZero": 0,
                    "ctrlsOcc": 0,
                    "ctrlsStranded": 0,
                    "starved": 2,
                    "small": 10,
                    "axeSerious": 0,
                    "axeCritical": 0,
                    "knownDebt": "2 starved scroll containers \u2014 hard floors are cardsOcc 0 / axeSerious 0. The `nested-interactive` serious that used to sit here is GONE (issue #2593): the deck row was `<div role=\"button\" tabIndex={0}>` wrapped around the Select button, and it is now an inert container with one absolutely-positioned overlay button carrying the row-wide gesture (`deck-list-item.tsx`). `cardsOcc 1` is NOT a deck tile: it is the same probe artifact as the `design-system-dialog` row, and it is NONDETERMINISTIC. `LobbyBackground` renders `AmbientPageGround` (`src/components/lobby/lobby-background.tsx:13`), whose decorative full-bleed `<img data-ambient-art aria-hidden>` draws its `src` at random per mount from `AMBIENT_BG_IMAGES` (8 local `/img/lobby-bg/*.webp` + 21 `cards.scryfall.io` art crops); only a Scryfall draw matches `probe.js:137-141`'s `img[src*=\"scryfall\"]`, and it then scores occluded behind the opaque signal panels. Measured on one unchanged tree: `n39/occ1 -> n38/occ0` between consecutive runs; pinning the draw to a local frame gives `n38/occ0` at every viewport. The ceiling is held at 1 so a Scryfall draw does not red the lane. Drafted for #2580's lane owner in `docs/findings/2581-ui-gate-card-probe-counts-ambient-backdrop.md`. `small10` at this touch viewport (`pointer: coarse` -> the 44px `--control-h-coarse` branch, `src/index.css:942,946-948`) is REAL sub-44px tap-target debt, not a measurement artifact. Recorded as a ceiling (issue #2658) so it cannot deepen; #2659 owns lowering it as the surface's controls are fixed."
                },
                "844x390x3": {
                    "cardsZero": 0,
                    "cardsOcc": 1,
                    "cardsStranded": 0,
                    "ctrlsZero": 0,
                    "ctrlsOcc": 0,
                    "ctrlsStranded": 0,
                    "starved": 2,
                    "small": 10,
                    "axeSerious": 0,
                    "axeCritical": 0,
                    "knownDebt": "2 starved scroll containers \u2014 hard floors are cardsOcc 0 / axeSerious 0. The `nested-interactive` serious that used to sit here is GONE (issue #2593): the deck row was `<div role=\"button\" tabIndex={0}>` wrapped around the Select button, and it is now an inert container with one absolutely-positioned overlay button carrying the row-wide gesture (`deck-list-item.tsx`). `cardsOcc 1` is NOT a deck tile: it is the same probe artifact as the `design-system-dialog` row, and it is NONDETERMINISTIC. `LobbyBackground` renders `AmbientPageGround` (`src/components/lobby/lobby-background.tsx:13`), whose decorative full-bleed `<img data-ambient-art aria-hidden>` draws its `src` at random per mount from `AMBIENT_BG_IMAGES` (8 local `/img/lobby-bg/*.webp` + 21 `cards.scryfall.io` art crops); only a Scryfall draw matches `probe.js:137-141`'s `img[src*=\"scryfall\"]`, and it then scores occluded behind the opaque signal panels. Measured on one unchanged tree: `n39/occ1 -> n38/occ0` between consecutive runs; pinning the draw to a local frame gives `n38/occ0` at every viewport. The ceiling is held at 1 so a Scryfall draw does not red the lane. Drafted for #2580's lane owner in `docs/findings/2581-ui-gate-card-probe-counts-ambient-backdrop.md`. `small10` at this touch viewport (`pointer: coarse` -> the 44px `--control-h-coarse` branch, `src/index.css:942,946-948`) is REAL sub-44px tap-target debt, not a measurement artifact. Recorded as a ceiling (issue #2658) so it cannot deepen; #2659 owns lowering it as the surface's controls are fixed."
                },
                "820x1180x2": {
                    "cardsZero": 0,
                    "cardsOcc": 1,
                    "cardsStranded": 0,
                    "ctrlsZero": 0,
                    "ctrlsOcc": 0,
                    "ctrlsStranded": 0,
                    "starved": 2,
                    "small": 22,
                    "axeSerious": 0,
                    "axeCritical": 0,
                    "knownDebt": "2 starved scroll containers \u2014 hard floors are cardsOcc 0 / axeSerious 0. The `nested-interactive` serious that used to sit here is GONE (issue #2593): the deck row was `<div role=\"button\" tabIndex={0}>` wrapped around the Select button, and it is now an inert container with one absolutely-positioned overlay button carrying the row-wide gesture (`deck-list-item.tsx`). `cardsOcc 1` is NOT a deck tile: it is the same probe artifact as the `design-system-dialog` row, and it is NONDETERMINISTIC. `LobbyBackground` renders `AmbientPageGround` (`src/components/lobby/lobby-background.tsx:13`), whose decorative full-bleed `<img data-ambient-art aria-hidden>` draws its `src` at random per mount from `AMBIENT_BG_IMAGES` (8 local `/img/lobby-bg/*.webp` + 21 `cards.scryfall.io` art crops); only a Scryfall draw matches `probe.js:137-141`'s `img[src*=\"scryfall\"]`, and it then scores occluded behind the opaque signal panels. Measured on one unchanged tree: `n39/occ1 -> n38/occ0` between consecutive runs; pinning the draw to a local frame gives `n38/occ0` at every viewport. The ceiling is held at 1 so a Scryfall draw does not red the lane. Drafted for #2580's lane owner in `docs/findings/2581-ui-gate-card-probe-counts-ambient-backdrop.md`. `small22` at this touch viewport (`pointer: coarse` -> the 44px `--control-h-coarse` branch, `src/index.css:942,946-948`) is REAL sub-44px tap-target debt, not a measurement artifact. Recorded as a ceiling (issue #2658) so it cannot deepen; #2659 owns lowering it as the surface's controls are fixed."
                },
                "1180x820x2": {
                    "cardsZero": 0,
                    "cardsOcc": 1,
                    "cardsStranded": 0,
                    "ctrlsZero": 0,
                    "ctrlsOcc": 0,
                    "ctrlsStranded": 0,
                    "starved": 2,
                    "small": 15,
                    "axeSerious": 0,
                    "axeCritical": 0,
                    "knownDebt": "2 starved scroll containers \u2014 hard floors are cardsOcc 0 / axeSerious 0. The `nested-interactive` serious that used to sit here is GONE (issue #2593): the deck row was `<div role=\"button\" tabIndex={0}>` wrapped around the Select button, and it is now an inert container with one absolutely-positioned overlay button carrying the row-wide gesture (`deck-list-item.tsx`). `cardsOcc 1` is NOT a deck tile: it is the same probe artifact as the `design-system-dialog` row, and it is NONDETERMINISTIC. `LobbyBackground` renders `AmbientPageGround` (`src/components/lobby/lobby-background.tsx:13`), whose decorative full-bleed `<img data-ambient-art aria-hidden>` draws its `src` at random per mount from `AMBIENT_BG_IMAGES` (8 local `/img/lobby-bg/*.webp` + 21 `cards.scryfall.io` art crops); only a Scryfall draw matches `probe.js:137-141`'s `img[src*=\"scryfall\"]`, and it then scores occluded behind the opaque signal panels. Measured on one unchanged tree: `n39/occ1 -> n38/occ0` between consecutive runs; pinning the draw to a local frame gives `n38/occ0` at every viewport. The ceiling is held at 1 so a Scryfall draw does not red the lane. Drafted for #2580's lane owner in `docs/findings/2581-ui-gate-card-probe-counts-ambient-backdrop.md`. `small15` at this touch viewport (`pointer: coarse` -> the 44px `--control-h-coarse` branch, `src/index.css:942,946-948`) is REAL sub-44px tap-target debt, not a measurement artifact. Recorded as a ceiling (issue #2658) so it cannot deepen; #2659 owns lowering it as the surface's controls are fixed."
                }
            }
        },
        "deck-builder": {
            "label": "Constructed deck builder (/decks/create)",
            "status": "budgeted",
            "viewports": {
                "1440x900x2": {
                    "cardsZero": 0,
                    "cardsOcc": 0,
                    "cardsStranded": 0,
                    "ctrlsZero": 1,
                    "ctrlsOcc": 0,
                    "ctrlsStranded": 0,
                    "starved": 0,
                    "small": 50,
                    "axeSerious": 0,
                    "axeCritical": 0,
                    "knownDebt": "Re-recorded 2026-08-20 (issue #2585 review fixup, PR #2653): `ctrlsStranded` (was 7) is 0 \u2014 review finding #2's one-token fix, dropping ` md:` from `md:shrink-0` on the zone toolbar's trailing cluster (`deck-zone-surface.tsx`), lets that cluster shrink and wrap at every width instead of pinning to max-content inside the dock-narrowed Sideboard pane. Confirmed with `bun run check:ui -- --surface=deck-builder`: n50 zero1 occ0 stranded0, identical small/tiny counts. The deck pane measures 640.5/900 = 71.2% idle and 594.5/900 = 66.1% with a search active, both clearing the issue's 60% floor with real margin. The dock's `Add Basic` trigger stays a 73x22px click target (round-3 fixup reverted a `minHeight: var(--control-h)` experiment: it reached 44px here but cost this same pane its idle/search-active share, and at 1180x820 it dropped the search-active reading below the 60% floor \u2014 see `docs/findings/2585-add-basic-dock-trigger-touch-target.md`). The \u226544px `pointer: coarse` target is tracked by #2670 (\"a11y: coarse-pointer targets below the ADR 0101 \u00a72 44px rung \u2014 #2593's unmet AC, currently drawer-only\", a #2405 sub-issue): #2593 (WCAG 2.2 AA touch-target reconciliation across `src/components/deckbuilder/**`) is CLOSED and shipped with that AC UNMET, and the census it left in `docs/findings/2593-coarse-pointer-touch-targets-below-44px.md` is #2670's starting point \u2014 whoever picks up #2670 can trade this 22px against the rest of the dock's chrome instead of borrowing against #2585's own AC. Issue #2593 cleared this surface's axe debt at every viewport and the floors are now hard: the `color-contrast` serious was the 10px `Auto-saved` label at `text-accent/70` (#927637 on surface-base, 4.43:1 \u2014 under the 4.5:1 floor by 0.07), raised to `/85` (6.04:1); the `scrollable-region-focusable` serious was the source pane (`data-deck-pane=\"source\"`), which now carries its own `tabIndex={0}` `role=\"region\"` tab stop. `small` is re-recorded 68 -> 49 on this branch: the dock moves the source panel's own controls (the pool's filter/sort chrome) out of the shared vertical stack, and the `Add Basic` bar folds behind one trigger in dock mode, so 19 sub-rung controls stop being mounted here at all. The ceiling follows the measurement down \u2014 leaving it at main's 68 would bank headroom this layout no longer needs. #2670 owns the 49 that remain. RE-MEASURED 2026-08-24 (issue #2671): the `/decks/create` walk now imports a fixed 5-card decklist (`scripts/ui-gate/surfaces.ts`, one card per grouping bucket so no Column pile stacks and paints a false `cardsOcc`/`ctrlsOcc`) instead of leaving both zones empty. `small` rises 49 -> 50: `DeckFeaturedSelect`'s \"Featured card\" native `<select>` (`deck-featured-select.tsx`) renders nothing on an empty Maindeck (\"Nothing renders on an empty Maindeck \u2014 there is no card to feature yet\"), so this 135x30 control was invisible to every walk before this issue closed the fixture hole. Real, pre-existing sub-44px debt in #2659/#2670's territory, not caused by the toolbar-fold fix \u2014 the ceiling follows the measurement up. Every other figure is unchanged (cardsOcc0/ctrlsOcc0/ctrlsStranded0/starved0): `1440x900x2` is landscape, outside `CompactChromeDisclosure`'s new `TABLET_PORTRAIT_QUERY` OR-condition (`compact-chrome-disclosure.tsx`), so the trailing cluster keeps wrapping verbatim exactly as before \u2014 `ctrlsStranded` stays the hard floor of 0. FIXUP 2026-08-24 (issue #2671 review round 1, PR #2755): unaffected by both M1 (pool-basic-lands-bar.tsx label fix) and M2 (TABLET_PORTRAIT_QUERY max-height bound) \u2014 this cell is landscape, so `useIsTabletPortrait()` never matches here regardless of the bound; confirmed byte-identical to the pre-fixup numbers (`ctrlsStranded0` unchanged)."
                },
                "390x844x3": {
                    "cardsZero": 0,
                    "cardsOcc": 0,
                    "cardsStranded": 0,
                    "ctrlsZero": 0,
                    "ctrlsOcc": 0,
                    "ctrlsStranded": 0,
                    "starved": 0,
                    "small": 12,
                    "axeSerious": 0,
                    "axeCritical": 0,
                    "knownDebt": "Re-recorded 2026-08-20 (issue #2585 fixup, PR #2650): clean at every measured floor. Was 1 collapsed + 1 occluded control + 1 axe serious (scrollable-region-focusable) before the filter controls left the header band for a bottom sheet. Issue #2593 cleared this surface's axe debt at every viewport and the floors are now hard: the `color-contrast` serious was the 10px `Auto-saved` label at `text-accent/70` (#927637 on surface-base, 4.43:1 \u2014 under the 4.5:1 floor by 0.07), raised to `/85` (6.04:1); the `scrollable-region-focusable` serious was the source pane (`data-deck-pane=\"source\"`), which now carries its own `tabIndex={0}` `role=\"region\"` tab stop. `small12` at this touch viewport (`pointer: coarse` -> the 44px `--control-h-coarse` branch, `src/index.css:942,946-948`) is REAL sub-44px tap-target debt, not a measurement artifact. Recorded as a ceiling (issue #2658) so it cannot deepen; #2659 owns lowering it as the surface's controls are fixed. RE-MEASURED 2026-08-24 (issue #2671): the `/decks/create` walk now imports a fixed 5-card decklist instead of leaving both zones empty (`scripts/ui-gate/surfaces.ts`); every figure at this viewport is byte-identical, including `small12` \u2014 `SaveDeckBar`'s new \"Featured card\" select (see the 1440x900x2 cell's note) does not mount in portrait (`!portrait` gate, `deck-builder-shell.tsx`), and this viewport's toolbar was already fully folded before this issue (`useViewportMode() === \"portrait\"`, unaffected by `CompactChromeDisclosure`'s new OR-condition). Also fixed here: `DeckMiniCurve`'s per-bar sparkline `<span>` carried a prohibited `aria-label` (bare `<span>` has no role that supports one) that a real, non-empty Maindeck now mounts for the first time (the component renders nothing on an empty deck) \u2014 dropped in favour of the existing `title` + the parent `role=\"group\"` label, matching `DeckStatsCurveChart`'s own pattern (`deck-mini-curve.tsx`). FIXUP 2026-08-24 (issue #2671 review round 1, PR #2755): unaffected by M1/M2 (both target the tablet-portrait `useIsTabletPortrait()` predicate, which never applies below `min-width: 768px` \u2014 this viewport's fold is `useViewportMode()`'s own portrait/landscape-compact bucket, untouched); byte-identical to the pre-fixup numbers. Also verified H2: the `deck-builder` walk's fixture import is now self-cleaning (`scripts/ui-gate/surfaces.ts`'s new `cleanup()` on this surface, wired in `index.ts`) \u2014 it deletes the userDecks row it created, on the SAME page, right after the probe measures this cell, by finding it on the lobby's My Decks list by its auto-assigned name and using the row's own overflow-menu Delete + confirm. Measured on the live deployment: My Decks row count before and after two consecutive full `--surface=deck-builder` runs (5 viewports each) is 51 -> 51 -> 51 \u2014 no residue. The pre-existing pollution from earlier development runs of this fixture (31 flagged by review + 2 more from this fixup's own manual verification, 33 total) was purged via the lobby's Delete affordance, content-matched against the fixture's exact Maindeck+Sideboard card names -- the \"Deck 36\"-\"Deck 70\" range those runs left behind (issue #2671 review round 2, L2 / review round 3): the purge MISSED 4 rows inside that range sharing the same fixture payload (\"Deck 37\"/\"Deck 45\"/\"Deck 54\"/\"Deck 61\"), plus \"Deck 71\" above it, so 5 inert fixture rows survive. They cannot be later-run output: `nextDeckName` (`src/lib/userDecks.ts`) is max(N)+1 over a single userId, so \"Deck 37\" could only be minted while \"Deck 36\" still existed. They are harmless residue, not pollution the lane keeps adding to -- `cleanup()` now runs on the failure path too, so the source is closed. Every pre-existing deck from before this issue (\"BEFORE #1617 -- mono red\", \"Sealed Pool Deck\", the ten \"UI Verify 2584 (throwaway)\" rows, Deck 1-35) survived untouched."
                },
                "844x390x3": {
                    "cardsZero": 0,
                    "cardsOcc": 0,
                    "cardsStranded": 0,
                    "ctrlsZero": 0,
                    "ctrlsOcc": 1,
                    "ctrlsStranded": 0,
                    "starved": 0,
                    "small": 19,
                    "axeSerious": 0,
                    "axeCritical": 0,
                    "knownDebt": "Re-recorded 2026-08-20 (issue #2585 fixup, PR #2650). Phone LANDSCAPE opens filters as a bottom sheet (`useSurfaceClass` phone branch), so this cell measures the header's own action row, not the sheet. `ctrlsOcc 1`: the search box (`search-bar.tsx`) -- at this width the header row (Back/Import/Export/Format select/Cube filter/Search/Filters) has no spare width left once the Filters button (issue #2585) joins it, so the box's flex item shrinks toward 0 and its `<input>` (whose intrinsic min-content width the browser holds near 34px regardless) would otherwise paint over the Filters button; the wrapper now carries `overflow-hidden` so that clips instead of occluding it. The probe hit-tests the `<input>`'s own layout rect because its `overflow-hidden` ancestor is not a SCROLL port (`scrollPort()` only recognises `auto`/`scroll`) -- a real pointer still reaches Filters cleanly, so this is not stranded. Before this fixup the cell measured `ctrlsOcc 5`: the header's action row wrapped the new Filters button onto a second full-height line (its label was un-collapsed text, unlike its `Stats` sibling), pushing the Maindeck's `Delete column <Lands|MV 0..3>` row down into `SaveDeckBar`'s band -- verified against `origin/main` at the same account/deck state, which measured `ctrlsOcc 0` there, so this was a genuine regression, not a legitimate new count. Fixed by hiding the Filters label under `short-viewport:` (icon-only, matching `Stats`) plus the search-bar clip above. Issue #2593 cleared this surface's axe debt at every viewport and the floors are now hard: the `color-contrast` serious was the 10px `Auto-saved` label at `text-accent/70` (#927637 on surface-base, 4.43:1 \u2014 under the 4.5:1 floor by 0.07), raised to `/85` (6.04:1); the `scrollable-region-focusable` serious was the source pane (`data-deck-pane=\"source\"`), which now carries its own `tabIndex={0}` `role=\"region\"` tab stop. `small` re-recorded 25 -> 27 (issue #2662, PR fix/issue-2662): `/decks/create` is registered `mode: \"immersive\"` (`shellChrome.ts`), so this surface's header IS `AppContextBar`, and #2662 deliberately shrinks its Exit link and overflow trigger from `--control-h` (44px, `pointer: coarse` on this touch viewport) to `--control-h-xs` (28px) under `short-viewport:` \u2014 the coarse-pointer 44px figure being traded is the AAA/iOS COMFORT target (ADR 0101 \u00a72), not the WCAG 2.2 AA MINIMUM (24x24 CSS px, SC 2.5.8) this probe's flat 44px `small` test cannot distinguish from. Both controls clear the AA floor (28 >= 24) but read as `small` by this probe's pointer-blind 44px cut, exactly as flagged in the ticket. This is a DIFFERENT two controls than the pre-existing `small25` debt below (this surface's own header/toolbar/search chrome, still #2659's to lower) \u2014 the ceiling rises by exactly the 2 newly-shrunk contextual-bar controls, not a regression in the pre-existing debt. `small25` at this touch viewport (`pointer: coarse` -> the 44px `--control-h-coarse` branch, `src/index.css:942,946-948`) was REAL sub-44px tap-target debt, not a measurement artifact, and #2659 still owns lowering that portion; #2662 owns only the +2 documented here. RE-MEASURED 2026-08-22 (issue #2665, the landscape-phone Column rung): `small` LOWERED 25 -> 16 at this viewport (the cell records 18: #2662 landed first and added 2) and nowhere else. The nine sub-44px controls that went away are the per-Column DELETE buttons (11px glyphs in the Column header row -- `DeckColumnActions` receives `onRename` only when `column.kind === \"manual\"` (`deck-zone-surface.tsx`, the `actions` slot), and this fixture's Columns are all generated, so there is no rename glyph among them): the header is not rendered on this rung any more, so its `actions` slot is not passed either, and the buttons are GONE from the document rather than hidden at zero size. The other four viewports on this surface measured byte-identical to the pre-change tree. RE-MEASURED 2026-08-24 (issue #2671): the walk now imports a fixed 5-card decklist instead of leaving both zones empty (`scripts/ui-gate/surfaces.ts`) \u2014 `small` rises 18 -> 19, the same newly-visible `DeckFeaturedSelect` \"Featured card\" native select (see the 1440x900x2 cell's note). `ctrlsOcc` stays 1, the pre-existing search-box debt documented above, unaffected by a populated deck. This viewport's toolbar was already fully folded before this issue (`useViewportMode() === \"landscape-compact\"`, unaffected by `CompactChromeDisclosure`'s new OR-condition). FIXUP 2026-08-24 (issue #2671 review round 1, PR #2755): unaffected by M1/M2 (both target the tablet-portrait `useIsTabletPortrait()` predicate, which never applies below `min-width: 768px` \u2014 this viewport's fold is `useViewportMode()`'s own portrait/landscape-compact bucket, untouched); byte-identical to the pre-fixup numbers. Also verified H2: the `deck-builder` walk's fixture import is now self-cleaning (`scripts/ui-gate/surfaces.ts`'s new `cleanup()` on this surface, wired in `index.ts`) \u2014 it deletes the userDecks row it created, on the SAME page, right after the probe measures this cell, by finding it on the lobby's My Decks list by its auto-assigned name and using the row's own overflow-menu Delete + confirm. Measured on the live deployment: My Decks row count before and after two consecutive full `--surface=deck-builder` runs (5 viewports each) is 51 -> 51 -> 51 \u2014 no residue. The pre-existing pollution from earlier development runs of this fixture (31 flagged by review + 2 more from this fixup's own manual verification, 33 total) was purged via the lobby's Delete affordance, content-matched against the fixture's exact Maindeck+Sideboard card names -- the \"Deck 36\"-\"Deck 70\" range those runs left behind (issue #2671 review round 2, L2 / review round 3): the purge MISSED 4 rows inside that range sharing the same fixture payload (\"Deck 37\"/\"Deck 45\"/\"Deck 54\"/\"Deck 61\"), plus \"Deck 71\" above it, so 5 inert fixture rows survive. They cannot be later-run output: `nextDeckName` (`src/lib/userDecks.ts`) is max(N)+1 over a single userId, so \"Deck 37\" could only be minted while \"Deck 36\" still existed. They are harmless residue, not pollution the lane keeps adding to -- `cleanup()` now runs on the failure path too, so the source is closed. Every pre-existing deck from before this issue (\"BEFORE #1617 -- mono red\", \"Sealed Pool Deck\", the ten \"UI Verify 2584 (throwaway)\" rows, Deck 1-35) survived untouched."
                },
                "820x1180x2": {
                    "cardsZero": 0,
                    "cardsOcc": 0,
                    "cardsStranded": 0,
                    "ctrlsZero": 1,
                    "ctrlsOcc": 0,
                    "ctrlsStranded": 0,
                    "starved": 0,
                    "small": 24,
                    "axeSerious": 0,
                    "axeCritical": 0,
                    "knownDebt": "Re-recorded 2026-08-20 (issue #2585 review fixup round 2, PR #2653): `ctrlsOcc` (was 2) and `ctrlsStranded` (was 9) are 0 \u2014 review finding #2's one-token fix (dropping ` md:` from `md:shrink-0` on the zone toolbar's trailing cluster, `deck-zone-surface.tsx`) applies at every width, unconditionally, so it clears this PRE-EXISTING tablet-portrait debt too even though this cell never matches `deck-source-dock:` (portrait excludes it). Confirmed with `bun run check:ui -- --surface=deck-builder`: n69 zero1 occ0 stranded0. `starved` stayed 0 ON THIS SURFACE \u2014 but it was a TRADE, not a pure win: /decks/create (what this lane walked) started every deck with an EMPTY Sideboard, and an empty zone has no card tile to starve against. A/B on that branch with a real (non-empty) Sideboard, same one `md:shrink-0` token differing, nothing else: header row 86px to 203px, card scroll port 300px to 183px \u2014 below the ~196px card tile (--card-h), the probe's own starved shape. See docs/findings/2585-sideboard-toolbar-clips-under-narrower-zones-width.md addendum for the full measurement. Issue #2593 cleared this surface's axe debt at every viewport and the floors are now hard: the `color-contrast` serious was the 10px `Auto-saved` label at `text-accent/70` (#927637 on surface-base, 4.43:1 \u2014 under the 4.5:1 floor by 0.07), raised to `/85` (6.04:1); the `scrollable-region-focusable` serious was the source pane (`data-deck-pane=\"source\"`), which now carries its own `tabIndex={0}` `role=\"region\"` tab stop. FIXED 2026-08-24 (issue #2671): `CompactChromeDisclosure` (`compact-chrome-disclosure.tsx`) now folds the trailing cluster here too, OR'd with a new `useIsTabletPortrait()` predicate \u2014 `TABLET_PORTRAIT_QUERY = \"(orientation: portrait) and (min-width: 768px)\"`, the complement of `useViewportMode`'s own portrait band. Chosen because `deck-source-dock` (`index.css`) never engages in portrait (it is landscape-only), so the zones pane's height budget stays tight at ANY portrait width `useViewportMode()` still calls `\"desktop\"` \u2014 there is no landscape-only story to bound it, so the predicate has no upper width bound of its own. Measured live (CDP `getBoundingClientRect()`, `/decks/create`, Freeform) with a REAL non-empty Sideboard for the first time \u2014 the `/decks/create` walk now imports a fixed 5-card decklist (`scripts/ui-gate/surfaces.ts`) instead of leaving both zones empty, closing the fixture hole this addendum flagged: Sideboard header row 203px -> 34px, card scroll port ~183px (interpolated on the old empty-Sideboard fixture) -> 384.5px \u2014 both comfortably clear the issue's 86px/300px AC (measured against a ~196-228px `--card-h` tile depending on the exact card). `starved0` at this cell is now load-bearing (a real Sideboard, not an empty one) rather than the pre-existing caveat above. `small` DROPS 65 -> 24: the same fold removes ~40 sub-44px controls from the flat DOM (colour toggles, grouping/ordering selects, zoom sliders) that a phone-shaped viewport already folds away \u2014 the ceiling follows the measurement down, the same convention as the 1440x900x2 cell's own 68 -> 49 drop. `ctrlsStranded`/`ctrlsOcc` stay the hard floor of 0 \u2014 `md:shrink-0` is NOT restored (AC). `CompactChromeDisclosure`'s other call site (`pool-basic-lands-bar.tsx`, the ADD BASIC bar) inherits the same OR-condition and now folds here too, which only helps: it recovers MORE height for the zones pane, not less. Confirmed with `bun run check:ui -- --surface=deck-builder`. FIXUP 2026-08-24 (issue #2671 review round 1, PR #2755). Two findings, both verified live, neither moves this cell's numbers: (M1) the OTHER CompactChromeDisclosure call site (`pool-basic-lands-bar.tsx`'s ADD BASIC bar) used to hide its static \"Add Basic\" label with `compact-chrome:hidden` \u2014 a CSS variant that never widened to cover `useIsTabletPortrait()`, so at this viewport the label stayed on screen beside the disclosure's own now-folded \"Add Basic \u25be\" toggle (measured `addBasicToggles=1` AND a visible static span=1). Fixed by reading the disclosure's OWN fold predicate (`useCompactChromeFold`, exported from `compact-chrome-disclosure.tsx`) to conditionally render the span instead of a second, independently-drifting CSS rule \u2014 confirmed live: exactly one \"Add Basic\" text node at this viewport, `ctrlsOcc0` unaffected. (M2) `TABLET_PORTRAIT_QUERY` gained an upper `max-height: 1300px` bound \u2014 the unbounded version folded even a 1440x2560 portrait viewport where the measured Sideboard card port (1105px) is ~4x the room the unfolded header needs, because the fold's premise (\"the pane's height budget stays tight at ANY portrait width\") conflated width-independence (true) with height-independence (false \u2014 the pane is height-linear, per the deck-source-dock equal-flex-1-sibling split). The bound sits ~80px past the measured crossover (\u22481219px at this cell's width) so it still folds here: confirmed live at this exact viewport post-fix, header 34px / port 393px (includes this session's own +18.5px \"active game\" banner offset; clean-baseline reads ~375px, still clearing the ~196-228px tile with margin), `starved0`. `small24` unchanged."
                },
                "1180x820x2": {
                    "cardsZero": 0,
                    "cardsOcc": 0,
                    "cardsStranded": 0,
                    "ctrlsZero": 1,
                    "ctrlsOcc": 0,
                    "ctrlsStranded": 0,
                    "starved": 0,
                    "small": 47,
                    "axeSerious": 0,
                    "axeCritical": 0,
                    "knownDebt": "Re-recorded 2026-08-20 (issue #2585 review fixup, PR #2653). `ctrlsOcc` (was 2) and `ctrlsStranded` (was 9) are 0 \u2014 review finding #2's one-token fix (dropping ` md:` from `md:shrink-0` on the zone toolbar's trailing cluster) clears the Sideboard-toolbar overflow-clip here the same way it does at 1440x900x2. `starved` stays 0 (the card-pile strip no longer shares its height with the source panel). The deck pane measures 552.5/820 = 67.4% idle / 494.5/820 = 60.3% search-active \u2014 both clear the issue's 60% floor. Round-2 briefly applied a `minHeight: var(--control-h)` treatment to the dock's `Add Basic` trigger (73x22px \u2192 73x44px, closing an ADR 0101 touch-target gap) but it borrowed that ~22px from this same flex column, dropping the search-active reading to 58.0% \u2014 below the AC floor; round-3 reverted it, restoring the figures above (A/B and root cause: `docs/findings/2585-add-basic-dock-trigger-touch-target.md`). The trigger stays a 22px-tall click target; reaching \u226544px at `pointer: coarse` here was #2593's AC (WCAG 2.2 AA, `src/components/deckbuilder/**`) but #2593 is CLOSED and shipped that AC UNMET \u2014 the gap is now tracked by #2670 (\"a11y: coarse-pointer targets below the ADR 0101 \u00a72 44px rung \u2014 #2593's unmet AC, currently drawer-only\", a #2405 sub-issue), whose census starts at `docs/findings/2593-coarse-pointer-touch-targets-below-44px.md`; whoever picks up #2670 can reclaim chrome elsewhere in this column instead of spending #2585's own AC margin. Issue #2593 cleared this surface's axe debt at every viewport and the floors are now hard: the `color-contrast` serious was the 10px `Auto-saved` label at `text-accent/70` (#927637 on surface-base, 4.43:1 \u2014 under the 4.5:1 floor by 0.07), raised to `/85` (6.04:1); the `scrollable-region-focusable` serious was the source pane (`data-deck-pane=\"source\"`), which now carries its own `tabIndex={0}` `role=\"region\"` tab stop. `small` is re-recorded 65 -> 46 on this branch, same dock cause as the 1440x900x2 cell above (source-panel chrome leaves the shared stack, the `Add Basic` bar folds behind one trigger). #2670 owns the 46 that remain, the 73x22px dock trigger among them. RE-MEASURED 2026-08-24 (issue #2671): the walk now imports a fixed 5-card decklist instead of leaving both zones empty (`scripts/ui-gate/surfaces.ts`) \u2014 `small` rises 46 -> 47, the same newly-visible `DeckFeaturedSelect` \"Featured card\" native select (see the 1440x900x2 cell's note). Every other figure is unchanged: `1180x820x2` is landscape, outside `CompactChromeDisclosure`'s new `TABLET_PORTRAIT_QUERY` OR-condition (`compact-chrome-disclosure.tsx`), so the trailing cluster keeps wrapping verbatim exactly as before \u2014 `ctrlsStranded` stays the hard floor of 0, matching the AC that this issue must not fold away controls at this viewport. FIXUP 2026-08-24 (issue #2671 review round 1, PR #2755): unaffected by both M1 (pool-basic-lands-bar.tsx label fix) and M2 (TABLET_PORTRAIT_QUERY max-height bound) \u2014 this cell is landscape, so `useIsTabletPortrait()` never matches here regardless of the bound; confirmed byte-identical to the pre-fixup numbers (`ctrlsStranded0` unchanged)."
                }
            }
        },
        "design-system": {
            "label": "Design system census (/admin/design-system)",
            "status": "budgeted",
            "viewports": {
                "1440x900x2": {
                    "cardsZero": 0,
                    "cardsOcc": 0,
                    "cardsStranded": 0,
                    "ctrlsZero": 0,
                    "ctrlsOcc": 0,
                    "ctrlsStranded": 0,
                    "starved": 1,
                    "small": 24,
                    "axeSerious": 0,
                    "axeCritical": 0,
                    "knownDebt": "PRE-EXISTING census-page debt: 1 starved container (a table in its own `overflow-x-auto`). The axe rows are 0/0 as of issue #2593 and are now hard floors. Two of the three violations were REAL and were fixed on the page: the unlabelled `<input>` specimens in section 09 now carry `aria-label`, and every `overflow-x-auto` table scroller now carries a `tabIndex={0}` `role=\"region\"` tab stop. The third is not a defect at all \u2014 the palette section renders retired hexes (#6f6244, #b1473a) beside their replacements, and the board's raw counter fills, ON PURPOSE, to show what a failing ratio looks like. Those three subtrees carry `data-axe-exempt=\"<why>\"` and the lane excludes them, printing `exempt3` on this surface's line of every run. The attribute cannot spread: `scripts/__tests__/axe-exemption-scope.test.ts` fails when it appears outside `src/routes/design-system/`. `small23` at 1440x900x2 (pointer: fine) is the deliberate 32px `--control-h-fine` branch (`src/index.css:942,946-948`) showing up in a POINTER-BLIND measurement, not a defect \u2014 WCAG 2.5.8 (cited in that CSS comment) is a TOUCH-target rule and does not bind a mouse pointer. Do NOT lower this ceiling by shrinking desktop controls; #2659 (the sibling fix issue) does not touch this row. RE-RECORDED 2026-08-25 (issue #2722, identity v4 tokens): `small` +1 at EVERY viewport, from 23 to 24. The +1 is the page's own table of contents \u2014 this slice adds section \u201c15 Identity v4\u201d, and the TOC renders one `<a href=\"#identity-v4\">` per section in a wrap row of ~18px-tall text links (`design-system.route.tsx`), every one of them inside the first viewport band and therefore counted. It is the same pointer-blind sub-44px measurement the note above describes, not new debt: a TOC link is a text anchor, and padding it to 44px would push the census's own navigation to three rows on a phone. Attribution is measured, not assumed \u2014 `bun run check:ui -- --surface=design-system` on the branch base (55d32f07) scored 23 at this viewport and PASSED, and the branch scores 24; every OTHER check:ui failure on this branch (`limited-list`, `limited-your-events`, `draft-pool-stop`) reproduces byte-identically on that same unmodified base and is untouched by this diff. The token swap itself moved NOTHING here: cards/ctrls zero/occ/stranded, `starved` and both axe floors are unchanged at every viewport \u2014 including axe `color-contrast`, which a whole-palette change would have surfaced first."
                },
                "390x844x3": {
                    "cardsZero": 0,
                    "cardsOcc": 0,
                    "cardsStranded": 0,
                    "ctrlsZero": 0,
                    "ctrlsOcc": 0,
                    "ctrlsStranded": 0,
                    "starved": 1,
                    "small": 17,
                    "axeSerious": 0,
                    "axeCritical": 0,
                    "knownDebt": "PRE-EXISTING census-page debt: 1 starved container (a table in its own `overflow-x-auto`). The axe rows are 0/0 as of issue #2593 and are now hard floors. Two of the three violations were REAL and were fixed on the page: the unlabelled `<input>` specimens in section 09 now carry `aria-label`, and every `overflow-x-auto` table scroller now carries a `tabIndex={0}` `role=\"region\"` tab stop. The third is not a defect at all \u2014 the palette section renders retired hexes (#6f6244, #b1473a) beside their replacements, and the board's raw counter fills, ON PURPOSE, to show what a failing ratio looks like. Those three subtrees carry `data-axe-exempt=\"<why>\"` and the lane excludes them, printing `exempt3` on this surface's line of every run. The attribute cannot spread: `scripts/__tests__/axe-exemption-scope.test.ts` fails when it appears outside `src/routes/design-system/`. Phone portrait's extra `scrollable-region-focusable` is fixed by the same tab stops. `small16` at this touch viewport (`pointer: coarse` -> the 44px `--control-h-coarse` branch, `src/index.css:942,946-948`) is REAL sub-44px tap-target debt, not a measurement artifact. Recorded as a ceiling (issue #2658) so it cannot deepen; #2659 owns lowering it as the surface's controls are fixed. RE-RECORDED 2026-08-25 (issue #2722, identity v4 tokens): `small` +1 at EVERY viewport, from 16 to 17. The +1 is the page's own table of contents \u2014 this slice adds section \u201c15 Identity v4\u201d, and the TOC renders one `<a href=\"#identity-v4\">` per section in a wrap row of ~18px-tall text links (`design-system.route.tsx`), every one of them inside the first viewport band and therefore counted. It is the same pointer-blind sub-44px measurement the note above describes, not new debt: a TOC link is a text anchor, and padding it to 44px would push the census's own navigation to three rows on a phone. Attribution is measured, not assumed \u2014 `bun run check:ui -- --surface=design-system` on the branch base (55d32f07) scored 16 at this viewport and PASSED, and the branch scores 17; every OTHER check:ui failure on this branch (`limited-list`, `limited-your-events`, `draft-pool-stop`) reproduces byte-identically on that same unmodified base and is untouched by this diff. The token swap itself moved NOTHING here: cards/ctrls zero/occ/stranded, `starved` and both axe floors are unchanged at every viewport \u2014 including axe `color-contrast`, which a whole-palette change would have surfaced first."
                },
                "844x390x3": {
                    "cardsZero": 0,
                    "cardsOcc": 0,
                    "cardsStranded": 0,
                    "ctrlsZero": 0,
                    "ctrlsOcc": 0,
                    "ctrlsStranded": 0,
                    "starved": 1,
                    "small": 22,
                    "axeSerious": 0,
                    "axeCritical": 0,
                    "knownDebt": "PRE-EXISTING census-page debt: 1 starved container (a table in its own `overflow-x-auto`). The axe rows are 0/0 as of issue #2593 and are now hard floors. Two of the three violations were REAL and were fixed on the page: the unlabelled `<input>` specimens in section 09 now carry `aria-label`, and every `overflow-x-auto` table scroller now carries a `tabIndex={0}` `role=\"region\"` tab stop. The third is not a defect at all \u2014 the palette section renders retired hexes (#6f6244, #b1473a) beside their replacements, and the board's raw counter fills, ON PURPOSE, to show what a failing ratio looks like. Those three subtrees carry `data-axe-exempt=\"<why>\"` and the lane excludes them, printing `exempt3` on this surface's line of every run. The attribute cannot spread: `scripts/__tests__/axe-exemption-scope.test.ts` fails when it appears outside `src/routes/design-system/`. `small21` at this touch viewport (`pointer: coarse` -> the 44px `--control-h-coarse` branch, `src/index.css:942,946-948`) is REAL sub-44px tap-target debt, not a measurement artifact. Recorded as a ceiling (issue #2658) so it cannot deepen; #2659 owns lowering it as the surface's controls are fixed. RE-RECORDED 2026-08-25 (issue #2722, identity v4 tokens): `small` +1 at EVERY viewport, from 21 to 22. The +1 is the page's own table of contents \u2014 this slice adds section \u201c15 Identity v4\u201d, and the TOC renders one `<a href=\"#identity-v4\">` per section in a wrap row of ~18px-tall text links (`design-system.route.tsx`), every one of them inside the first viewport band and therefore counted. It is the same pointer-blind sub-44px measurement the note above describes, not new debt: a TOC link is a text anchor, and padding it to 44px would push the census's own navigation to three rows on a phone. Attribution is measured, not assumed \u2014 `bun run check:ui -- --surface=design-system` on the branch base (55d32f07) scored 21 at this viewport and PASSED, and the branch scores 22; every OTHER check:ui failure on this branch (`limited-list`, `limited-your-events`, `draft-pool-stop`) reproduces byte-identically on that same unmodified base and is untouched by this diff. The token swap itself moved NOTHING here: cards/ctrls zero/occ/stranded, `starved` and both axe floors are unchanged at every viewport \u2014 including axe `color-contrast`, which a whole-palette change would have surfaced first."
                },
                "820x1180x2": {
                    "cardsZero": 0,
                    "cardsOcc": 0,
                    "cardsStranded": 0,
                    "ctrlsZero": 0,
                    "ctrlsOcc": 0,
                    "ctrlsStranded": 0,
                    "starved": 1,
                    "small": 22,
                    "axeSerious": 0,
                    "axeCritical": 0,
                    "knownDebt": "PRE-EXISTING census-page debt: 1 starved container (a table in its own `overflow-x-auto`). The axe rows are 0/0 as of issue #2593 and are now hard floors. Two of the three violations were REAL and were fixed on the page: the unlabelled `<input>` specimens in section 09 now carry `aria-label`, and every `overflow-x-auto` table scroller now carries a `tabIndex={0}` `role=\"region\"` tab stop. The third is not a defect at all \u2014 the palette section renders retired hexes (#6f6244, #b1473a) beside their replacements, and the board's raw counter fills, ON PURPOSE, to show what a failing ratio looks like. Those three subtrees carry `data-axe-exempt=\"<why>\"` and the lane excludes them, printing `exempt3` on this surface's line of every run. The attribute cannot spread: `scripts/__tests__/axe-exemption-scope.test.ts` fails when it appears outside `src/routes/design-system/`. `small21` at this touch viewport (`pointer: coarse` -> the 44px `--control-h-coarse` branch, `src/index.css:942,946-948`) is REAL sub-44px tap-target debt, not a measurement artifact. Recorded as a ceiling (issue #2658) so it cannot deepen; #2659 owns lowering it as the surface's controls are fixed. RE-RECORDED 2026-08-25 (issue #2722, identity v4 tokens): `small` +1 at EVERY viewport, from 21 to 22. The +1 is the page's own table of contents \u2014 this slice adds section \u201c15 Identity v4\u201d, and the TOC renders one `<a href=\"#identity-v4\">` per section in a wrap row of ~18px-tall text links (`design-system.route.tsx`), every one of them inside the first viewport band and therefore counted. It is the same pointer-blind sub-44px measurement the note above describes, not new debt: a TOC link is a text anchor, and padding it to 44px would push the census's own navigation to three rows on a phone. Attribution is measured, not assumed \u2014 `bun run check:ui -- --surface=design-system` on the branch base (55d32f07) scored 21 at this viewport and PASSED, and the branch scores 22; every OTHER check:ui failure on this branch (`limited-list`, `limited-your-events`, `draft-pool-stop`) reproduces byte-identically on that same unmodified base and is untouched by this diff. The token swap itself moved NOTHING here: cards/ctrls zero/occ/stranded, `starved` and both axe floors are unchanged at every viewport \u2014 including axe `color-contrast`, which a whole-palette change would have surfaced first."
                },
                "1180x820x2": {
                    "cardsZero": 0,
                    "cardsOcc": 0,
                    "cardsStranded": 0,
                    "ctrlsZero": 0,
                    "ctrlsOcc": 0,
                    "ctrlsStranded": 0,
                    "starved": 1,
                    "small": 22,
                    "axeSerious": 0,
                    "axeCritical": 0,
                    "knownDebt": "PRE-EXISTING census-page debt: 1 starved container (a table in its own `overflow-x-auto`). The axe rows are 0/0 as of issue #2593 and are now hard floors. Two of the three violations were REAL and were fixed on the page: the unlabelled `<input>` specimens in section 09 now carry `aria-label`, and every `overflow-x-auto` table scroller now carries a `tabIndex={0}` `role=\"region\"` tab stop. The third is not a defect at all \u2014 the palette section renders retired hexes (#6f6244, #b1473a) beside their replacements, and the board's raw counter fills, ON PURPOSE, to show what a failing ratio looks like. Those three subtrees carry `data-axe-exempt=\"<why>\"` and the lane excludes them, printing `exempt3` on this surface's line of every run. The attribute cannot spread: `scripts/__tests__/axe-exemption-scope.test.ts` fails when it appears outside `src/routes/design-system/`. `small21` at this touch viewport (`pointer: coarse` -> the 44px `--control-h-coarse` branch, `src/index.css:942,946-948`) is REAL sub-44px tap-target debt, not a measurement artifact. Recorded as a ceiling (issue #2658) so it cannot deepen; #2659 owns lowering it as the surface's controls are fixed. RE-RECORDED 2026-08-25 (issue #2722, identity v4 tokens): `small` +1 at EVERY viewport, from 21 to 22. The +1 is the page's own table of contents \u2014 this slice adds section \u201c15 Identity v4\u201d, and the TOC renders one `<a href=\"#identity-v4\">` per section in a wrap row of ~18px-tall text links (`design-system.route.tsx`), every one of them inside the first viewport band and therefore counted. It is the same pointer-blind sub-44px measurement the note above describes, not new debt: a TOC link is a text anchor, and padding it to 44px would push the census's own navigation to three rows on a phone. Attribution is measured, not assumed \u2014 `bun run check:ui -- --surface=design-system` on the branch base (55d32f07) scored 21 at this viewport and PASSED, and the branch scores 22; every OTHER check:ui failure on this branch (`limited-list`, `limited-your-events`, `draft-pool-stop`) reproduces byte-identically on that same unmodified base and is untouched by this diff. The token swap itself moved NOTHING here: cards/ctrls zero/occ/stranded, `starved` and both axe floors are unchanged at every viewport \u2014 including axe `color-contrast`, which a whole-palette change would have surfaced first."
                }
            }
        },
        "design-system-dialog": {
            "label": "GameDialog live demo (/admin/design-system \u2192 Open live demo)",
            "status": "budgeted",
            "viewports": {
                "1440x900x2": {
                    "cardsZero": 0,
                    "cardsOcc": 1,
                    "cardsStranded": 0,
                    "ctrlsZero": 0,
                    "ctrlsOcc": 14,
                    "ctrlsStranded": 0,
                    "starved": 1,
                    "small": 16,
                    "axeSerious": 0,
                    "axeCritical": 0,
                    "knownDebt": "The lane's only MODAL row (#2581): with the GameDialog demo open, 10-12 controls behind the scrim measure as occluded \u2014 that is what a modal IS, not a defect, and `ctrlsOcc` is not one of the lane's hard floors. `cardsOcc` on this row is a PROBE ARTIFACT and it is NONDETERMINISTIC \u2014 do not read it as a card. The census page renders no card specimen at all: no `<img>` exists anywhere under `src/routes/design-system/`. What `probe.js:137-141` (`img[src*=\"scryfall\"]`) actually catches here is `AmbientPageGround`'s decorative full-bleed backdrop `<img data-ambient-art aria-hidden>` (`src/components/ui/ambient-page-ground.tsx:111-115`, mounted by `src/routes/design-system.route.tsx:36`), whose `src` is drawn AT RANDOM on every mount (`pickRandom`, same file :7-9) from `AMBIENT_BG_IMAGES` (`src/components/ui/ambient-backgrounds.ts:66`) \u2014 8 local `/img/lobby-bg/*.webp` plus 21 `cards.scryfall.io` art crops, and ONLY the latter match the selector. So `cards n` on this surface is 1 on roughly 21/29 of runs and 0 otherwise, and when it is 1 it scores occluded because the backdrop sits behind the scrim. Measured flapping on one unchanged tree (`6b1c944c`): two runs gave `cards n0 occ0` here, `design-system @1440x900x2` went `n1 reach1 -> n0`, and `lobby` went `n39/occ1 -> n38/occ0`; pinning `pickRandom()` to a fixed local frame drove `cards n0` on all ten census/dialog rows. The ceiling is held at 1 rather than lowered to 0 precisely because 0 would red the lane whenever the draw yields a Scryfall frame. The selector hole belongs to #2580's lane \u2014 an `aria-hidden` decorative backdrop must not be counted as a card \u2014 and is drafted in `docs/findings/2581-ui-gate-card-probe-counts-ambient-backdrop.md`; fixing it re-baselines every `cards*` row in the lane, so it ships in its own PR, not this one. `starved 1` is the census page's own table scroller, unchanged from the `design-system` row. axe is clean (s0/c0) WITH the dialog open, which is the assertion this row exists for: the Panel v3 frame introduces no violation. RE-RECORDED by issue #2582 (PR #2619, review round 3) against the CORRECTED probe. TWO things moved this row and the note has to name both. (1) The shell's `AppReturnBanner` adds ONE control to this page \u2014 its `Return to game` / `Return to event` button \u2014 and it sits behind the same scrim as the controls already counted here, so it lands in `ctrlsOcc` for exactly the reason they do. The band renders only while a game or a Limited event is in flight on the deployment, so it is present in these readings and absent on a quiet one. (2) The probe's hit test moved TWICE inside this PR. The first cut clipped the test to the element's scroll port but still tested its RAW CENTRE, which silently reclassified two genuinely-covered controls as `reachable`; round 3 replaced it with the centre of the element's VISIBLE INTERSECTION with (viewport n port), and `scrollPort` now stops at a `position: fixed` box instead of walking past it into an ancestor scroller. Every number below is what THAT probe measures \u2014 enumerated per element with `elementFromPoint`, not inferred, and identical across two enumerator runs and a full lane run on this tree. This also RETIRES the previous note's `11, 12 and 13 have all been measured` caveat, which blamed page scroll: the flapping was the raw-centre knife edge. Section 08's `[data-slot=panel-footer]` Cancel/Confirm specimens paint at y 76-106 against a `<main>` whose clip edge is y 92, so a raw-centre test decided both on a 1px margin and flipped them together, 13 <-> 11. The intersection centre lands at y 99, 7px inside the painted strip, and does not flip. THIS ROW MEASURES 13, enumerated: TOLARIA / Home / Limited / Admin / Test Fil / Sign out (the `AppHeader` row, all six above `<main>`'s y=92 edge and under the scrim), `Return to game` (`[data-slot=app-return-banner]`, r=[1323,63,1428,85]), the section-08 `Cancel` r=[543,76,606,106] and `Confirm` r=[614,76,687,106] Panel specimens (each with 14px painted below the clip edge \u2014 these are the two the raw-centre probe lost), three `Open live demo` and `Report a bug`. Against `origin/main`'s recorded 12 that is +1, the banner button: the specimen pair is counted by the pre-#2582 probe and by the corrected one alike, so nothing is double-counted here. `ctrlsOcc` is not one of the lane's hard floors and this is the lane's only MODAL row, so a control behind the scrim is the assertion, not the defect. The ceiling is the MEASURED value: lower it again when the shell changes what the scrim covers, and never raise it without an enumeration in this note naming the control that was added. RE-RECORDED AGAIN by #2595 (PR #2620, post-merge fixup round 4, 2026-08-20 \u2014 `main` had gone red on this row). #2620 added ONE control to `AppHeader`'s profile cluster: a `Settings` button (`src/components/chrome/app-header-profile.tsx:167-176`, `title=\"Settings\"`, navigates to `/settings`, icon-only below the `sm` breakpoint via `hidden sm:inline` on its own text label) rendered between `Test Fil` and `Sign out`. It sits in the SAME header row as those controls and behind the SAME scrim, so it lands in `ctrlsOcc` for the same reason the other six header controls do. Verified by ENUMERATION, not inference: the corrected probe's per-element hit test (`elementFromPoint` over the visible intersection with viewport \u2229 scroll port, identical logic to `probe.js`) was run on this tree (`9b97e1f9`, includes #2620) and diffed element-by-element against the archived pre-#2620 enumeration (`.claude/receipts/session_016Ky4htNSsWuNL2jwVDKFYd/2582-fixup-3-occ-enumeration.log`, produced for PR #2619 / commit `24c02d63`, whose counts \u2014 13/8/11/11/11 \u2014 match what `origin/main` had recorded before this fixup). At every one of the four failing viewports the OCC set differs from that archive by EXACTLY one entry, `Settings`; every other OCC element (rect, slot, hit target) is unchanged. Two unrelated REACHABLE (never occluded) controls also appeared page-wide at every viewport in this run \u2014 a `Done`/`\u2715` pair, part of #2595's own density/motion/phase-stop preview surface elsewhere on the census page, off-screen and not behind the scrim \u2014 they do not touch `ctrlsOcc` and are named here only because the diff was audited element-by-element rather than assumed. THIS ROW MEASURES 14, enumerated: TOLARIA / Home / Limited / Admin / Test Fil / Settings r=[1064,12,1157,44] / Sign out (all seven `AppHeader` controls, now including `Settings`, above `<main>`'s y=92 edge and under the scrim), `Return to game` r=[1323,63,1428,85], the section-08 `Cancel` r=[543,76,606,106] and `Confirm` r=[614,76,687,106] Panel specimens, three `Open live demo`, `Report a bug`. Against the prior round's recorded 13 that is +1, `Settings`, and nothing else. `390x844x3` is UNAFFECTED, still 8: at that viewport the shell renders in phone-portrait/bottom-nav mode (#2619) and mounts no `AppHeader` at all, so `AppHeaderProfile` (where `Settings` lives) never mounts \u2014 there is no `Settings` control on that surface to occlude. #2595's `hidden sm:inline` only hides the button's TEXT LABEL below `sm`; it plays no part in this absence \u2014 the button is missing here because the whole header is missing, not because of the icon-only breakpoint. The profile/settings affordance on phone portrait already lives in the bottom nav's `Me` popover, counted in this row before #2620 and unchanged by it. `ctrlsOcc` is not one of the lane's hard floors and this is the lane's only MODAL row, so a control behind the scrim is the assertion, not the defect. The ceiling is the MEASURED value: lower it again when the shell changes what the scrim covers, and never raise it without an enumeration in this note naming the control that was added. `small16` at 1440x900x2 (pointer: fine) is the deliberate 32px `--control-h-fine` branch (`src/index.css:942,946-948`) showing up in a POINTER-BLIND measurement, not a defect \u2014 WCAG 2.5.8 (cited in that CSS comment) is a TOUCH-target rule and does not bind a mouse pointer. Do NOT lower this ceiling by shrinking desktop controls; #2659 (the sibling fix issue) does not touch this row."
                },
                "390x844x3": {
                    "cardsZero": 0,
                    "cardsOcc": 1,
                    "cardsStranded": 0,
                    "ctrlsZero": 0,
                    "ctrlsOcc": 8,
                    "ctrlsStranded": 0,
                    "starved": 1,
                    "small": 7,
                    "axeSerious": 0,
                    "axeCritical": 0,
                    "knownDebt": "The lane's only MODAL row (#2581): with the GameDialog demo open, 10-12 controls behind the scrim measure as occluded \u2014 that is what a modal IS, not a defect, and `ctrlsOcc` is not one of the lane's hard floors. `cardsOcc` on this row is a PROBE ARTIFACT and it is NONDETERMINISTIC \u2014 do not read it as a card. The census page renders no card specimen at all: no `<img>` exists anywhere under `src/routes/design-system/`. What `probe.js:137-141` (`img[src*=\"scryfall\"]`) actually catches here is `AmbientPageGround`'s decorative full-bleed backdrop `<img data-ambient-art aria-hidden>` (`src/components/ui/ambient-page-ground.tsx:111-115`, mounted by `src/routes/design-system.route.tsx:36`), whose `src` is drawn AT RANDOM on every mount (`pickRandom`, same file :7-9) from `AMBIENT_BG_IMAGES` (`src/components/ui/ambient-backgrounds.ts:66`) \u2014 8 local `/img/lobby-bg/*.webp` plus 21 `cards.scryfall.io` art crops, and ONLY the latter match the selector. So `cards n` on this surface is 1 on roughly 21/29 of runs and 0 otherwise, and when it is 1 it scores occluded because the backdrop sits behind the scrim. Measured flapping on one unchanged tree (`6b1c944c`): two runs gave `cards n0 occ0` here, `design-system @1440x900x2` went `n1 reach1 -> n0`, and `lobby` went `n39/occ1 -> n38/occ0`; pinning `pickRandom()` to a fixed local frame drove `cards n0` on all ten census/dialog rows. The ceiling is held at 1 rather than lowered to 0 precisely because 0 would red the lane whenever the draw yields a Scryfall frame. The selector hole belongs to #2580's lane \u2014 an `aria-hidden` decorative backdrop must not be counted as a card \u2014 and is drafted in `docs/findings/2581-ui-gate-card-probe-counts-ambient-backdrop.md`; fixing it re-baselines every `cards*` row in the lane, so it ships in its own PR, not this one. `starved 1` is the census page's own table scroller, unchanged from the `design-system` row. axe is clean (s0/c0) WITH the dialog open, which is the assertion this row exists for: the Panel v3 frame introduces no violation. RE-RECORDED by issue #2582 (PR #2619, review round 3) against the CORRECTED probe. TWO things moved this row and the note has to name both. (1) The shell's `AppReturnBanner` adds ONE control to this page \u2014 its `Return to game` / `Return to event` button \u2014 and it sits behind the same scrim as the controls already counted here, so it lands in `ctrlsOcc` for exactly the reason they do. The band renders only while a game or a Limited event is in flight on the deployment, so it is present in these readings and absent on a quiet one. (2) The probe's hit test moved TWICE inside this PR. The first cut clipped the test to the element's scroll port but still tested its RAW CENTRE, which silently reclassified two genuinely-covered controls as `reachable`; round 3 replaced it with the centre of the element's VISIBLE INTERSECTION with (viewport n port), and `scrollPort` now stops at a `position: fixed` box instead of walking past it into an ancestor scroller. Every number below is what THAT probe measures \u2014 enumerated per element with `elementFromPoint`, not inferred, and identical across two enumerator runs and a full lane run on this tree. This also RETIRES the previous note's `11, 12 and 13 have all been measured` caveat, which blamed page scroll: the flapping was the raw-centre knife edge. Section 08's `[data-slot=panel-footer]` Cancel/Confirm specimens paint at y 76-106 against a `<main>` whose clip edge is y 92, so a raw-centre test decided both on a 1px margin and flipped them together, 13 <-> 11. The intersection centre lands at y 99, 7px inside the painted strip, and does not flip. THIS ROW MEASURES 8, enumerated: `Return to game` r=[273,7,378,29], three `Open live demo`, the three bottom-nav items `Play` / `Limited` / `Me` (`[data-slot=app-bottom-nav]`, y 789-844 \u2014 below `<main>`'s 0-788 box, `position: fixed` so no port, and under the same full-viewport scrim) and `Report a bug`. The phone shell renders NO `AppHeader` row at all \u2014 issue #2582 replaced those six controls with the three-item bottom nav \u2014 which is why this row is the lowest in the matrix. `origin/main` recorded 10; 10 -> 8 because the shell traded six occluded header controls for three occluded nav ones, +1 banner. `ctrlsOcc` is not one of the lane's hard floors and this is the lane's only MODAL row, so a control behind the scrim is the assertion, not the defect. The ceiling is the MEASURED value: lower it again when the shell changes what the scrim covers, and never raise it without an enumeration in this note naming the control that was added. `small7` at this touch viewport (`pointer: coarse` -> the 44px `--control-h-coarse` branch, `src/index.css:942,946-948`) is REAL sub-44px tap-target debt, not a measurement artifact. Recorded as a ceiling (issue #2658) so it cannot deepen; #2659 owns lowering it as the surface's controls are fixed."
                },
                "844x390x3": {
                    "cardsZero": 0,
                    "cardsOcc": 0,
                    "cardsStranded": 0,
                    "ctrlsZero": 0,
                    "ctrlsOcc": 12,
                    "ctrlsStranded": 0,
                    "starved": 1,
                    "small": 12,
                    "axeSerious": 0,
                    "axeCritical": 0,
                    "knownDebt": "The lane's only MODAL row (#2581): with the GameDialog demo open, 10-12 controls behind the scrim measure as occluded \u2014 that is what a modal IS, not a defect, and `ctrlsOcc` is not one of the lane's hard floors. `cardsOcc` on this row is a PROBE ARTIFACT and it is NONDETERMINISTIC \u2014 do not read it as a card. The census page renders no card specimen at all: no `<img>` exists anywhere under `src/routes/design-system/`. What `probe.js:137-141` (`img[src*=\"scryfall\"]`) actually catches here is `AmbientPageGround`'s decorative full-bleed backdrop `<img data-ambient-art aria-hidden>` (`src/components/ui/ambient-page-ground.tsx:111-115`, mounted by `src/routes/design-system.route.tsx:36`), whose `src` is drawn AT RANDOM on every mount (`pickRandom`, same file :7-9) from `AMBIENT_BG_IMAGES` (`src/components/ui/ambient-backgrounds.ts:66`) \u2014 8 local `/img/lobby-bg/*.webp` plus 21 `cards.scryfall.io` art crops, and ONLY the latter match the selector. So `cards n` on this surface is 1 on roughly 21/29 of runs and 0 otherwise, and when it is 1 it scores occluded because the backdrop sits behind the scrim. Measured flapping on one unchanged tree (`6b1c944c`): two runs gave `cards n0 occ0` here, `design-system @1440x900x2` went `n1 reach1 -> n0`, and `lobby` went `n39/occ1 -> n38/occ0`; pinning `pickRandom()` to a fixed local frame drove `cards n0` on all ten census/dialog rows. The ceiling is held at 1 rather than lowered to 0 precisely because 0 would red the lane whenever the draw yields a Scryfall frame. The selector hole belongs to #2580's lane \u2014 an `aria-hidden` decorative backdrop must not be counted as a card \u2014 and is drafted in `docs/findings/2581-ui-gate-card-probe-counts-ambient-backdrop.md`; fixing it re-baselines every `cards*` row in the lane, so it ships in its own PR, not this one. `starved 1` is the census page's own table scroller, unchanged from the `design-system` row. axe is clean (s0/c0) WITH the dialog open, which is the assertion this row exists for: the Panel v3 frame introduces no violation. RE-RECORDED by issue #2582 (PR #2619, review round 3) against the CORRECTED probe. TWO things moved this row and the note has to name both. (1) The shell's `AppReturnBanner` adds ONE control to this page \u2014 its `Return to game` / `Return to event` button \u2014 and it sits behind the same scrim as the controls already counted here, so it lands in `ctrlsOcc` for exactly the reason they do. The band renders only while a game or a Limited event is in flight on the deployment, so it is present in these readings and absent on a quiet one. (2) The probe's hit test moved TWICE inside this PR. The first cut clipped the test to the element's scroll port but still tested its RAW CENTRE, which silently reclassified two genuinely-covered controls as `reachable`; round 3 replaced it with the centre of the element's VISIBLE INTERSECTION with (viewport n port), and `scrollPort` now stops at a `position: fixed` box instead of walking past it into an ancestor scroller. Every number below is what THAT probe measures \u2014 enumerated per element with `elementFromPoint`, not inferred, and identical across two enumerator runs and a full lane run on this tree. This also RETIRES the previous note's `11, 12 and 13 have all been measured` caveat, which blamed page scroll: the flapping was the raw-centre knife edge. Section 08's `[data-slot=panel-footer]` Cancel/Confirm specimens paint at y 76-106 against a `<main>` whose clip edge is y 92, so a raw-centre test decided both on a 1px margin and flipped them together, 13 <-> 11. The intersection centre lands at y 99, 7px inside the painted strip, and does not flip. THIS ROW MEASURES 11, enumerated: the six `AppHeader` controls, `Return to game` r=[727,47,832,69], three `Open live demo` and `Report a bug`. The section-08 Cancel/Confirm specimens do NOT appear: at this viewport the census page is scrolled so they sit entirely above `<main>`'s y=76 clip edge, nothing of them is painted, and they score `reachable`. `origin/main` recorded 10; 10 -> 11 is +1 for the banner button and nothing else. `ctrlsOcc` is not one of the lane's hard floors and this is the lane's only MODAL row, so a control behind the scrim is the assertion, not the defect. The ceiling is the MEASURED value: lower it again when the shell changes what the scrim covers, and never raise it without an enumeration in this note naming the control that was added. RE-RECORDED AGAIN by #2595 (PR #2620, post-merge fixup round 4, 2026-08-20 \u2014 `main` had gone red on this row). #2620 added ONE control to `AppHeader`'s profile cluster: a `Settings` button (`src/components/chrome/app-header-profile.tsx:167-176`, `title=\"Settings\"`, navigates to `/settings`, icon-only below the `sm` breakpoint via `hidden sm:inline` on its own text label) rendered between `Test Fil` and `Sign out`. It sits in the SAME header row as those controls and behind the SAME scrim, so it lands in `ctrlsOcc` for the same reason the other six header controls do. Verified by ENUMERATION, not inference: the corrected probe's per-element hit test (`elementFromPoint` over the visible intersection with viewport \u2229 scroll port, identical logic to `probe.js`) was run on this tree (`9b97e1f9`, includes #2620) and diffed element-by-element against the archived pre-#2620 enumeration (`.claude/receipts/session_016Ky4htNSsWuNL2jwVDKFYd/2582-fixup-3-occ-enumeration.log`, produced for PR #2619 / commit `24c02d63`, whose counts \u2014 13/8/11/11/11 \u2014 match what `origin/main` had recorded before this fixup). At every one of the four failing viewports the OCC set differs from that archive by EXACTLY one entry, `Settings`; every other OCC element (rect, slot, hit target) is unchanged. Two unrelated REACHABLE (never occluded) controls also appeared page-wide at every viewport in this run \u2014 a `Done`/`\u2715` pair, part of #2595's own density/motion/phase-stop preview surface elsewhere on the census page, off-screen and not behind the scrim \u2014 they do not touch `ctrlsOcc` and are named here only because the diff was audited element-by-element rather than assumed. THIS ROW MEASURES 12, enumerated: the seven `AppHeader` controls (now including `Settings` r=[656,-2,737,42]), `Return to game` r=[727,47,832,69], three `Open live demo`, `Report a bug`. The section-08 Cancel/Confirm specimens still do not appear at this viewport \u2014 the census page is scrolled so they sit entirely above `<main>`'s y=76 clip edge, scoring `reachable`, unchanged from the prior round. Against the prior round's recorded 11 that is +1, `Settings`, and nothing else. `390x844x3` is UNAFFECTED, still 8: at that viewport the shell renders in phone-portrait/bottom-nav mode (#2619) and mounts no `AppHeader` at all, so `AppHeaderProfile` (where `Settings` lives) never mounts \u2014 there is no `Settings` control on that surface to occlude. #2595's `hidden sm:inline` only hides the button's TEXT LABEL below `sm`; it plays no part in this absence \u2014 the button is missing here because the whole header is missing, not because of the icon-only breakpoint. The profile/settings affordance on phone portrait already lives in the bottom nav's `Me` popover, counted in this row before #2620 and unchanged by it. `ctrlsOcc` is not one of the lane's hard floors and this is the lane's only MODAL row, so a control behind the scrim is the assertion, not the defect. The ceiling is the MEASURED value: lower it again when the shell changes what the scrim covers, and never raise it without an enumeration in this note naming the control that was added. `small12` at this touch viewport (`pointer: coarse` -> the 44px `--control-h-coarse` branch, `src/index.css:942,946-948`) is REAL sub-44px tap-target debt, not a measurement artifact. Recorded as a ceiling (issue #2658) so it cannot deepen; #2659 owns lowering it as the surface's controls are fixed."
                },
                "820x1180x2": {
                    "cardsZero": 0,
                    "cardsOcc": 1,
                    "cardsStranded": 0,
                    "ctrlsZero": 0,
                    "ctrlsOcc": 12,
                    "ctrlsStranded": 0,
                    "starved": 1,
                    "small": 14,
                    "axeSerious": 0,
                    "axeCritical": 0,
                    "knownDebt": "The lane's only MODAL row (#2581): with the GameDialog demo open, 10-12 controls behind the scrim measure as occluded \u2014 that is what a modal IS, not a defect, and `ctrlsOcc` is not one of the lane's hard floors. `cardsOcc` on this row is a PROBE ARTIFACT and it is NONDETERMINISTIC \u2014 do not read it as a card. The census page renders no card specimen at all: no `<img>` exists anywhere under `src/routes/design-system/`. What `probe.js:137-141` (`img[src*=\"scryfall\"]`) actually catches here is `AmbientPageGround`'s decorative full-bleed backdrop `<img data-ambient-art aria-hidden>` (`src/components/ui/ambient-page-ground.tsx:111-115`, mounted by `src/routes/design-system.route.tsx:36`), whose `src` is drawn AT RANDOM on every mount (`pickRandom`, same file :7-9) from `AMBIENT_BG_IMAGES` (`src/components/ui/ambient-backgrounds.ts:66`) \u2014 8 local `/img/lobby-bg/*.webp` plus 21 `cards.scryfall.io` art crops, and ONLY the latter match the selector. So `cards n` on this surface is 1 on roughly 21/29 of runs and 0 otherwise, and when it is 1 it scores occluded because the backdrop sits behind the scrim. Measured flapping on one unchanged tree (`6b1c944c`): two runs gave `cards n0 occ0` here, `design-system @1440x900x2` went `n1 reach1 -> n0`, and `lobby` went `n39/occ1 -> n38/occ0`; pinning `pickRandom()` to a fixed local frame drove `cards n0` on all ten census/dialog rows. The ceiling is held at 1 rather than lowered to 0 precisely because 0 would red the lane whenever the draw yields a Scryfall frame. The selector hole belongs to #2580's lane \u2014 an `aria-hidden` decorative backdrop must not be counted as a card \u2014 and is drafted in `docs/findings/2581-ui-gate-card-probe-counts-ambient-backdrop.md`; fixing it re-baselines every `cards*` row in the lane, so it ships in its own PR, not this one. `starved 1` is the census page's own table scroller, unchanged from the `design-system` row. axe is clean (s0/c0) WITH the dialog open, which is the assertion this row exists for: the Panel v3 frame introduces no violation. RE-RECORDED by issue #2582 (PR #2619, review round 3) against the CORRECTED probe. TWO things moved this row and the note has to name both. (1) The shell's `AppReturnBanner` adds ONE control to this page \u2014 its `Return to game` / `Return to event` button \u2014 and it sits behind the same scrim as the controls already counted here, so it lands in `ctrlsOcc` for exactly the reason they do. The band renders only while a game or a Limited event is in flight on the deployment, so it is present in these readings and absent on a quiet one. (2) The probe's hit test moved TWICE inside this PR. The first cut clipped the test to the element's scroll port but still tested its RAW CENTRE, which silently reclassified two genuinely-covered controls as `reachable`; round 3 replaced it with the centre of the element's VISIBLE INTERSECTION with (viewport n port), and `scrollPort` now stops at a `position: fixed` box instead of walking past it into an ancestor scroller. Every number below is what THAT probe measures \u2014 enumerated per element with `elementFromPoint`, not inferred, and identical across two enumerator runs and a full lane run on this tree. This also RETIRES the previous note's `11, 12 and 13 have all been measured` caveat, which blamed page scroll: the flapping was the raw-centre knife edge. Section 08's `[data-slot=panel-footer]` Cancel/Confirm specimens paint at y 76-106 against a `<main>` whose clip edge is y 92, so a raw-centre test decided both on a 1px margin and flipped them together, 13 <-> 11. The intersection centre lands at y 99, 7px inside the painted strip, and does not flip. THIS ROW MEASURES 11, enumerated: the six `AppHeader` controls, `Return to game` r=[703,63,808,85], three `Open live demo` and `Report a bug`. The section-08 Cancel/Confirm specimens sit entirely above `<main>`'s y=92 clip edge here and score `reachable`. `origin/main` recorded 12, and the round-2 fixup raised this row to 13 by carrying the 1440x900x2 enumeration across \u2014 but no instrument this PR ships reads 13 here, on either side of the probe change. 12 -> 11 is the measurement: six header controls, the banner, and four page controls. `ctrlsOcc` is not one of the lane's hard floors and this is the lane's only MODAL row, so a control behind the scrim is the assertion, not the defect. The ceiling is the MEASURED value: lower it again when the shell changes what the scrim covers, and never raise it without an enumeration in this note naming the control that was added. RE-RECORDED AGAIN by #2595 (PR #2620, post-merge fixup round 4, 2026-08-20 \u2014 `main` had gone red on this row). #2620 added ONE control to `AppHeader`'s profile cluster: a `Settings` button (`src/components/chrome/app-header-profile.tsx:167-176`, `title=\"Settings\"`, navigates to `/settings`, icon-only below the `sm` breakpoint via `hidden sm:inline` on its own text label) rendered between `Test Fil` and `Sign out`. It sits in the SAME header row as those controls and behind the SAME scrim, so it lands in `ctrlsOcc` for the same reason the other six header controls do. Verified by ENUMERATION, not inference: the corrected probe's per-element hit test (`elementFromPoint` over the visible intersection with viewport \u2229 scroll port, identical logic to `probe.js`) was run on this tree (`9b97e1f9`, includes #2620) and diffed element-by-element against the archived pre-#2620 enumeration (`.claude/receipts/session_016Ky4htNSsWuNL2jwVDKFYd/2582-fixup-3-occ-enumeration.log`, produced for PR #2619 / commit `24c02d63`, whose counts \u2014 13/8/11/11/11 \u2014 match what `origin/main` had recorded before this fixup). At every one of the four failing viewports the OCC set differs from that archive by EXACTLY one entry, `Settings`; every other OCC element (rect, slot, hit target) is unchanged. Two unrelated REACHABLE (never occluded) controls also appeared page-wide at every viewport in this run \u2014 a `Done`/`\u2715` pair, part of #2595's own density/motion/phase-stop preview surface elsewhere on the census page, off-screen and not behind the scrim \u2014 they do not touch `ctrlsOcc` and are named here only because the diff was audited element-by-element rather than assumed. THIS ROW MEASURES 12, enumerated: the seven `AppHeader` controls (now including `Settings` r=[588,6,681,50]), `Return to game` r=[703,63,808,85], three `Open live demo`, `Report a bug`. The section-08 Cancel/Confirm specimens remain scrolled clear of `<main>`'s y=92 clip edge and score `reachable`, unchanged from the prior round. Against the prior round's recorded 11 that is +1, `Settings`, and nothing else. `390x844x3` is UNAFFECTED, still 8: at that viewport the shell renders in phone-portrait/bottom-nav mode (#2619) and mounts no `AppHeader` at all, so `AppHeaderProfile` (where `Settings` lives) never mounts \u2014 there is no `Settings` control on that surface to occlude. #2595's `hidden sm:inline` only hides the button's TEXT LABEL below `sm`; it plays no part in this absence \u2014 the button is missing here because the whole header is missing, not because of the icon-only breakpoint. The profile/settings affordance on phone portrait already lives in the bottom nav's `Me` popover, counted in this row before #2620 and unchanged by it. `ctrlsOcc` is not one of the lane's hard floors and this is the lane's only MODAL row, so a control behind the scrim is the assertion, not the defect. The ceiling is the MEASURED value: lower it again when the shell changes what the scrim covers, and never raise it without an enumeration in this note naming the control that was added. `small14` at this touch viewport (`pointer: coarse` -> the 44px `--control-h-coarse` branch, `src/index.css:942,946-948`) is REAL sub-44px tap-target debt, not a measurement artifact. Recorded as a ceiling (issue #2658) so it cannot deepen; #2659 owns lowering it as the surface's controls are fixed."
                },
                "1180x820x2": {
                    "cardsZero": 0,
                    "cardsOcc": 0,
                    "cardsStranded": 0,
                    "ctrlsZero": 0,
                    "ctrlsOcc": 12,
                    "ctrlsStranded": 0,
                    "starved": 1,
                    "small": 14,
                    "axeSerious": 0,
                    "axeCritical": 0,
                    "knownDebt": "The lane's only MODAL row (#2581): with the GameDialog demo open, 10-12 controls behind the scrim measure as occluded \u2014 that is what a modal IS, not a defect, and `ctrlsOcc` is not one of the lane's hard floors. `cardsOcc` on this row is a PROBE ARTIFACT and it is NONDETERMINISTIC \u2014 do not read it as a card. The census page renders no card specimen at all: no `<img>` exists anywhere under `src/routes/design-system/`. What `probe.js:137-141` (`img[src*=\"scryfall\"]`) actually catches here is `AmbientPageGround`'s decorative full-bleed backdrop `<img data-ambient-art aria-hidden>` (`src/components/ui/ambient-page-ground.tsx:111-115`, mounted by `src/routes/design-system.route.tsx:36`), whose `src` is drawn AT RANDOM on every mount (`pickRandom`, same file :7-9) from `AMBIENT_BG_IMAGES` (`src/components/ui/ambient-backgrounds.ts:66`) \u2014 8 local `/img/lobby-bg/*.webp` plus 21 `cards.scryfall.io` art crops, and ONLY the latter match the selector. So `cards n` on this surface is 1 on roughly 21/29 of runs and 0 otherwise, and when it is 1 it scores occluded because the backdrop sits behind the scrim. Measured flapping on one unchanged tree (`6b1c944c`): two runs gave `cards n0 occ0` here, `design-system @1440x900x2` went `n1 reach1 -> n0`, and `lobby` went `n39/occ1 -> n38/occ0`; pinning `pickRandom()` to a fixed local frame drove `cards n0` on all ten census/dialog rows. The ceiling is held at 1 rather than lowered to 0 precisely because 0 would red the lane whenever the draw yields a Scryfall frame. The selector hole belongs to #2580's lane \u2014 an `aria-hidden` decorative backdrop must not be counted as a card \u2014 and is drafted in `docs/findings/2581-ui-gate-card-probe-counts-ambient-backdrop.md`; fixing it re-baselines every `cards*` row in the lane, so it ships in its own PR, not this one. `starved 1` is the census page's own table scroller, unchanged from the `design-system` row. axe is clean (s0/c0) WITH the dialog open, which is the assertion this row exists for: the Panel v3 frame introduces no violation. RE-RECORDED by issue #2582 (PR #2619, review round 3) against the CORRECTED probe. TWO things moved this row and the note has to name both. (1) The shell's `AppReturnBanner` adds ONE control to this page \u2014 its `Return to game` / `Return to event` button \u2014 and it sits behind the same scrim as the controls already counted here, so it lands in `ctrlsOcc` for exactly the reason they do. The band renders only while a game or a Limited event is in flight on the deployment, so it is present in these readings and absent on a quiet one. (2) The probe's hit test moved TWICE inside this PR. The first cut clipped the test to the element's scroll port but still tested its RAW CENTRE, which silently reclassified two genuinely-covered controls as `reachable`; round 3 replaced it with the centre of the element's VISIBLE INTERSECTION with (viewport n port), and `scrollPort` now stops at a `position: fixed` box instead of walking past it into an ancestor scroller. Every number below is what THAT probe measures \u2014 enumerated per element with `elementFromPoint`, not inferred, and identical across two enumerator runs and a full lane run on this tree. This also RETIRES the previous note's `11, 12 and 13 have all been measured` caveat, which blamed page scroll: the flapping was the raw-centre knife edge. Section 08's `[data-slot=panel-footer]` Cancel/Confirm specimens paint at y 76-106 against a `<main>` whose clip edge is y 92, so a raw-centre test decided both on a 1px margin and flipped them together, 13 <-> 11. The intersection centre lands at y 99, 7px inside the painted strip, and does not flip. THIS ROW MEASURES 11, enumerated: the six `AppHeader` controls, `Return to game` r=[1063,63,1168,85], three `Open live demo` and `Report a bug`. As at 820x1180x2 the section-08 Cancel/Confirm specimens are scrolled clear of `<main>`'s y=92 clip edge and score `reachable`. `origin/main` recorded 12; 12 -> 11, correcting the round-2 fixup's 13, which no shipped instrument measures here. `ctrlsOcc` is not one of the lane's hard floors and this is the lane's only MODAL row, so a control behind the scrim is the assertion, not the defect. The ceiling is the MEASURED value: lower it again when the shell changes what the scrim covers, and never raise it without an enumeration in this note naming the control that was added. RE-RECORDED AGAIN by #2595 (PR #2620, post-merge fixup round 4, 2026-08-20 \u2014 `main` had gone red on this row). #2620 added ONE control to `AppHeader`'s profile cluster: a `Settings` button (`src/components/chrome/app-header-profile.tsx:167-176`, `title=\"Settings\"`, navigates to `/settings`, icon-only below the `sm` breakpoint via `hidden sm:inline` on its own text label) rendered between `Test Fil` and `Sign out`. It sits in the SAME header row as those controls and behind the SAME scrim, so it lands in `ctrlsOcc` for the same reason the other six header controls do. Verified by ENUMERATION, not inference: the corrected probe's per-element hit test (`elementFromPoint` over the visible intersection with viewport \u2229 scroll port, identical logic to `probe.js`) was run on this tree (`9b97e1f9`, includes #2620) and diffed element-by-element against the archived pre-#2620 enumeration (`.claude/receipts/session_016Ky4htNSsWuNL2jwVDKFYd/2582-fixup-3-occ-enumeration.log`, produced for PR #2619 / commit `24c02d63`, whose counts \u2014 13/8/11/11/11 \u2014 match what `origin/main` had recorded before this fixup). At every one of the four failing viewports the OCC set differs from that archive by EXACTLY one entry, `Settings`; every other OCC element (rect, slot, hit target) is unchanged. Two unrelated REACHABLE (never occluded) controls also appeared page-wide at every viewport in this run \u2014 a `Done`/`\u2715` pair, part of #2595's own density/motion/phase-stop preview surface elsewhere on the census page, off-screen and not behind the scrim \u2014 they do not touch `ctrlsOcc` and are named here only because the diff was audited element-by-element rather than assumed. THIS ROW MEASURES 12, enumerated: the seven `AppHeader` controls (now including `Settings` r=[934,6,1027,50]), `Return to game` r=[1063,63,1168,85], three `Open live demo`, `Report a bug`. As at 820x1180x2 the section-08 Cancel/Confirm specimens are scrolled clear of `<main>`'s y=92 clip edge and score `reachable`. Against the prior round's recorded 11 that is +1, `Settings`, and nothing else. `390x844x3` is UNAFFECTED, still 8: at that viewport the shell renders in phone-portrait/bottom-nav mode (#2619) and mounts no `AppHeader` at all, so `AppHeaderProfile` (where `Settings` lives) never mounts \u2014 there is no `Settings` control on that surface to occlude. #2595's `hidden sm:inline` only hides the button's TEXT LABEL below `sm`; it plays no part in this absence \u2014 the button is missing here because the whole header is missing, not because of the icon-only breakpoint. The profile/settings affordance on phone portrait already lives in the bottom nav's `Me` popover, counted in this row before #2620 and unchanged by it. `ctrlsOcc` is not one of the lane's hard floors and this is the lane's only MODAL row, so a control behind the scrim is the assertion, not the defect. The ceiling is the MEASURED value: lower it again when the shell changes what the scrim covers, and never raise it without an enumeration in this note naming the control that was added. `small14` at this touch viewport (`pointer: coarse` -> the 44px `--control-h-coarse` branch, `src/index.css:942,946-948`) is REAL sub-44px tap-target debt, not a measurement artifact. Recorded as a ceiling (issue #2658) so it cannot deepen; #2659 owns lowering it as the surface's controls are fixed."
                }
            }
        },
        "limited-list": {
            "label": "Limited events list (/limited)",
            "status": "budgeted",
            "viewports": {
                "1440x900x2": {
                    "cardsZero": 0,
                    "cardsOcc": 0,
                    "cardsStranded": 0,
                    "ctrlsZero": 0,
                    "ctrlsOcc": 0,
                    "ctrlsStranded": 0,
                    "starved": 0,
                    "small": 24,
                    "axeSerious": 0,
                    "axeCritical": 0,
                    "knownDebt": "`small24` at 1440x900x2 (pointer: fine) is the deliberate 32px `--control-h-fine` branch (`src/index.css:942,946-948`) showing up in a POINTER-BLIND measurement, not a defect \u2014 WCAG 2.5.8 (cited in that CSS comment) is a TOUCH-target rule and does not bind a mouse pointer. Do NOT lower this ceiling by shrinking desktop controls; #2659 (the sibling fix issue) does not touch this row."
                },
                "390x844x3": {
                    "cardsZero": 0,
                    "cardsOcc": 0,
                    "cardsStranded": 0,
                    "ctrlsZero": 0,
                    "ctrlsOcc": 0,
                    "ctrlsStranded": 0,
                    "starved": 0,
                    "small": 16,
                    "axeSerious": 0,
                    "axeCritical": 0,
                    "knownDebt": "`small16` at this touch viewport (`pointer: coarse` -> the 44px `--control-h-coarse` branch, `src/index.css:942,946-948`) is REAL sub-44px tap-target debt, not a measurement artifact. Recorded as a ceiling (issue #2658) so it cannot deepen; #2659 owns lowering it as the surface's controls are fixed."
                },
                "844x390x3": {
                    "cardsZero": 0,
                    "cardsOcc": 0,
                    "cardsStranded": 0,
                    "ctrlsZero": 0,
                    "ctrlsOcc": 0,
                    "ctrlsStranded": 0,
                    "starved": 1,
                    "small": 16,
                    "axeSerious": 0,
                    "axeCritical": 0,
                    "knownDebt": "`starved 1` at phone LANDSCAPE only, and it is `<main>` itself \u2014 NOT a component's window. The route renders one child, `div.mx-auto.flex.max-w-3xl.flex-col.gap-6.px-6`, which at 844 wide is 673px of event rows; `<main>` is the app's page scroller (issue #2056), so at 844x390 it is a 314px window onto a 673px column and `probe.js:76-91` starves at `clientHeight < 0.9 * tallest`. That is what page scrolling IS on a 390px-tall screen, and no shell height buys it off: clearing the threshold needs a 606px window out of 390px of viewport. MEASURED PRE-EXISTING, not introduced by issue #2582 \u2014 the same probe run against `origin/main`'s `src` on this tree and this deployment reports the same single starved `<main>` at this viewport (340px window, 673px column). What #2582 changes is only the window: the top bar goes 50px -> 40px (a WIN) and the return banner adds 36px while a game or event is in flight, so 340 -> 314. The recorded 0 dates from 2026-08-19, when this deployment's event list was short enough to fit \u2014 the row is data-dependent, which is the deeper reason it reads as debt. The `<main>`-as-starved false positive belongs to #2580's lane (the heuristic is for a 66px window around a 101px card tile, not for a scrolling page) and is drafted in `docs/findings/2582-ui-gate-main-scroller-starved.md`. `small16` at this touch viewport (`pointer: coarse` -> the 44px `--control-h-coarse` branch, `src/index.css:942,946-948`) is REAL sub-44px tap-target debt, not a measurement artifact. Recorded as a ceiling (issue #2658) so it cannot deepen; #2659 owns lowering it as the surface's controls are fixed."
                },
                "820x1180x2": {
                    "cardsZero": 0,
                    "cardsOcc": 0,
                    "cardsStranded": 0,
                    "ctrlsZero": 0,
                    "ctrlsOcc": 0,
                    "ctrlsStranded": 0,
                    "starved": 0,
                    "small": 21,
                    "axeSerious": 0,
                    "axeCritical": 0,
                    "knownDebt": "`small21` at this touch viewport (`pointer: coarse` -> the 44px `--control-h-coarse` branch, `src/index.css:942,946-948`) is REAL sub-44px tap-target debt, not a measurement artifact. Recorded as a ceiling (issue #2658) so it cannot deepen; #2659 owns lowering it as the surface's controls are fixed."
                },
                "1180x820x2": {
                    "cardsZero": 0,
                    "cardsOcc": 0,
                    "cardsStranded": 0,
                    "ctrlsZero": 0,
                    "ctrlsOcc": 0,
                    "ctrlsStranded": 0,
                    "starved": 0,
                    "small": 21,
                    "axeSerious": 0,
                    "axeCritical": 0,
                    "knownDebt": "`small21` at this touch viewport (`pointer: coarse` -> the 44px `--control-h-coarse` branch, `src/index.css:942,946-948`) is REAL sub-44px tap-target debt, not a measurement artifact. Recorded as a ceiling (issue #2658) so it cannot deepen; #2659 owns lowering it as the surface's controls are fixed."
                }
            }
        },
        "limited-your-events": {
            "label": "Your Limited events redirect (/limited/events \u2192 /limited?mine=1)",
            "status": "budgeted",
            "viewports": {
                "1440x900x2": {
                    "cardsZero": 0,
                    "cardsOcc": 0,
                    "cardsStranded": 0,
                    "ctrlsZero": 0,
                    "ctrlsOcc": 0,
                    "ctrlsStranded": 0,
                    "starved": 0,
                    "small": 22,
                    "axeSerious": 0,
                    "axeCritical": 0,
                    "knownDebt": "`small22` at 1440x900x2 (pointer: fine) is the deliberate 32px `--control-h-fine` branch (`src/index.css:942,946-948`) showing up in a POINTER-BLIND measurement, not a defect \u2014 WCAG 2.5.8 (cited in that CSS comment) is a TOUCH-target rule and does not bind a mouse pointer. Do NOT lower this ceiling by shrinking desktop controls; #2659 (the sibling fix issue) does not touch this row."
                },
                "390x844x3": {
                    "cardsZero": 0,
                    "cardsOcc": 0,
                    "cardsStranded": 0,
                    "ctrlsZero": 0,
                    "ctrlsOcc": 0,
                    "ctrlsStranded": 0,
                    "starved": 0,
                    "small": 15,
                    "axeSerious": 0,
                    "axeCritical": 0,
                    "knownDebt": "`small15` at this touch viewport (`pointer: coarse` -> the 44px `--control-h-coarse` branch, `src/index.css:942,946-948`) is REAL sub-44px tap-target debt, not a measurement artifact. Recorded as a ceiling (issue #2658) so it cannot deepen; #2659 owns lowering it as the surface's controls are fixed."
                },
                "844x390x3": {
                    "cardsZero": 0,
                    "cardsOcc": 0,
                    "cardsStranded": 0,
                    "ctrlsZero": 0,
                    "ctrlsOcc": 0,
                    "ctrlsStranded": 0,
                    "starved": 1,
                    "small": 16,
                    "axeSerious": 0,
                    "axeCritical": 0,
                    "knownDebt": "RE-RECORDED by issue #2590: `/limited/events` is now a redirect stub to `/limited?mine=1` (`limited-your-events.route.tsx`) \u2014 the your-events page this row used to measure no longer exists, and a visit here now lands on the SAME page `limited-list` measures, just filtered. `starved 1` at phone LANDSCAPE only is therefore the identical `<main>`-as-page-scroller shape `limited-list` carries at this viewport: the route renders one child column taller than the 314px window `<main>` gives it at 844x390 (top bar 40px + the return banner while a game/event is in flight), and `probe.js:76-91` starves at `clientHeight < 0.9 * tallest`. Not introduced by this issue \u2014 the same probe run against `origin/main`'s `src` on this tree and deployment reports the same single starved `<main>` here. The `<main>`-as-starved false positive belongs to #2580's lane (the heuristic is for a 66px window around a 101px card tile, not for a scrolling page) and is drafted in `docs/findings/2582-ui-gate-main-scroller-starved.md`. `small16` at this touch viewport (`pointer: coarse` -> the 44px `--control-h-coarse` branch, `src/index.css:942,946-948`) is REAL sub-44px tap-target debt, not a measurement artifact. Recorded as a ceiling (issue #2658) so it cannot deepen; #2659 owns lowering it as the surface's controls are fixed."
                },
                "820x1180x2": {
                    "cardsZero": 0,
                    "cardsOcc": 0,
                    "cardsStranded": 0,
                    "ctrlsZero": 0,
                    "ctrlsOcc": 0,
                    "ctrlsStranded": 0,
                    "starved": 0,
                    "small": 20,
                    "axeSerious": 0,
                    "axeCritical": 0,
                    "knownDebt": "`small20` at this touch viewport (`pointer: coarse` -> the 44px `--control-h-coarse` branch, `src/index.css:942,946-948`) is REAL sub-44px tap-target debt, not a measurement artifact. Recorded as a ceiling (issue #2658) so it cannot deepen; #2659 owns lowering it as the surface's controls are fixed."
                },
                "1180x820x2": {
                    "cardsZero": 0,
                    "cardsOcc": 0,
                    "cardsStranded": 0,
                    "ctrlsZero": 0,
                    "ctrlsOcc": 0,
                    "ctrlsStranded": 0,
                    "starved": 0,
                    "small": 20,
                    "axeSerious": 0,
                    "axeCritical": 0,
                    "knownDebt": "`small20` at this touch viewport (`pointer: coarse` -> the 44px `--control-h-coarse` branch, `src/index.css:942,946-948`) is REAL sub-44px tap-target debt, not a measurement artifact. Recorded as a ceiling (issue #2658) so it cannot deepen; #2659 owns lowering it as the surface's controls are fixed."
                }
            }
        },
        "limited-antechamber": {
            "label": "Limited event antechamber (/limited/<id>)",
            "status": "budgeted",
            "viewports": {
                "1440x900x2": {
                    "cardsZero": 0,
                    "cardsOcc": 0,
                    "cardsStranded": 0,
                    "ctrlsZero": 0,
                    "ctrlsOcc": 0,
                    "ctrlsStranded": 0,
                    "starved": 0,
                    "small": 15,
                    "axeSerious": 0,
                    "axeCritical": 0,
                    "knownDebt": "`small15` at 1440x900x2 (pointer: fine) is the deliberate 32px `--control-h-fine` branch (`src/index.css:942,946-948`) showing up in a POINTER-BLIND measurement, not a defect \u2014 WCAG 2.5.8 (cited in that CSS comment) is a TOUCH-target rule and does not bind a mouse pointer. Do NOT lower this ceiling by shrinking desktop controls; #2659 (the sibling fix issue) does not touch this row."
                },
                "390x844x3": {
                    "cardsZero": 0,
                    "cardsOcc": 0,
                    "cardsStranded": 0,
                    "ctrlsZero": 0,
                    "ctrlsOcc": 0,
                    "ctrlsStranded": 0,
                    "starved": 0,
                    "small": 4,
                    "axeSerious": 0,
                    "axeCritical": 0,
                    "knownDebt": "`small4` at this touch viewport (`pointer: coarse` -> the 44px `--control-h-coarse` branch, `src/index.css:942,946-948`) is REAL sub-44px tap-target debt, not a measurement artifact. Recorded as a ceiling (issue #2658) so it cannot deepen; #2659 owns lowering it as the surface's controls are fixed."
                },
                "844x390x3": {
                    "cardsZero": 0,
                    "cardsOcc": 0,
                    "cardsStranded": 0,
                    "ctrlsZero": 0,
                    "ctrlsOcc": 0,
                    "ctrlsStranded": 0,
                    "starved": 1,
                    "small": 9,
                    "axeSerious": 0,
                    "axeCritical": 0,
                    "knownDebt": "starved 1 at phone LANDSCAPE only, and it is the shared `<main>`-as-page-scroller shape (issue #2582's finding, docs/findings/2582-ui-gate-main-scroller-starved.md), not a defect this issue introduced. `LimitedEventDetail` sits inside the same `LimitedEventPageFrame` / `<main>` shell as `limited-list`, whose 844x390x3 row carries the identical note; verified directly (clientHeight 314 vs scrollHeight 546, same 314px window) rather than assumed. Clearing it needs a 546px window out of 390px of viewport, which is the app shell's job, not this surface's. `small9` at this touch viewport (`pointer: coarse` -> the 44px `--control-h-coarse` branch, `src/index.css:942,946-948`) is REAL sub-44px tap-target debt, not a measurement artifact. Recorded as a ceiling (issue #2658) so it cannot deepen; #2659 owns lowering it as the surface's controls are fixed."
                },
                "820x1180x2": {
                    "cardsZero": 0,
                    "cardsOcc": 0,
                    "cardsStranded": 0,
                    "ctrlsZero": 0,
                    "ctrlsOcc": 0,
                    "ctrlsStranded": 0,
                    "starved": 0,
                    "small": 9,
                    "axeSerious": 0,
                    "axeCritical": 0,
                    "knownDebt": "`small9` at this touch viewport (`pointer: coarse` -> the 44px `--control-h-coarse` branch, `src/index.css:942,946-948`) is REAL sub-44px tap-target debt, not a measurement artifact. Recorded as a ceiling (issue #2658) so it cannot deepen; #2659 owns lowering it as the surface's controls are fixed."
                },
                "1180x820x2": {
                    "cardsZero": 0,
                    "cardsOcc": 0,
                    "cardsStranded": 0,
                    "ctrlsZero": 0,
                    "ctrlsOcc": 0,
                    "ctrlsStranded": 0,
                    "starved": 0,
                    "small": 9,
                    "axeSerious": 0,
                    "axeCritical": 0,
                    "knownDebt": "`small9` at this touch viewport (`pointer: coarse` -> the 44px `--control-h-coarse` branch, `src/index.css:942,946-948`) is REAL sub-44px tap-target debt, not a measurement artifact. Recorded as a ceiling (issue #2658) so it cannot deepen; #2659 owns lowering it as the surface's controls are fixed."
                }
            }
        },
        "game-board": {
            "label": "Game board (/game)",
            "status": "unwalked",
            "reason": "MEASURED AND WITHDRAWN, on purpose. The walk works (surfaces.ts resumes a match or deals a solo game), but the position it lands on is not fixed, and two consecutive runs of the SAME tree gave cardsOcc 4 then 5 at 844x390x3 \u2014 hand-fan overlap scales with the hand. A ceiling that flaps is worse than no ceiling: a lane people learn to re-run is a lane they route around (#2512). The board becomes budgetable through `game-stress` below, which loads a FIXED position; close that row first, then this one."
        },
        "game-stress": {
            "label": "Game board \u2014 UI stress scenario",
            "status": "unwalked",
            "reason": "needs a game the lane created ITSELF (it refuses to load a debug scenario into a match it did not create \u2014 that would clobber someone's game) AND the `UI stress \u2014 full board, full hand, deep piles` row on the deployment. An active game was in progress when these budgets were recorded. Re-record from an empty lobby: `bun run check:ui -- --record --surface=game-stress`."
        },
        "limited-build": {
            "label": "Limited pool builder (/limited/<id>/build)",
            "status": "unwalked",
            "reason": "the only Limited event on this deployment is still in the DRAFTING phase, so no seat offers `Build Deck` yet. The walk is implemented in surfaces.ts (open the event from /limited, click Build Deck); delete this row and re-record once an event reaches `playing`. This is the surface the 2026-08-17 occlusion measurement came from, so it is the highest-value row to close."
        },
        "draft-pick": {
            "label": "Draft Room (/limited/<id>/draft)",
            "status": "budgeted",
            "viewports": {
                "1440x900x2": {
                    "cardsZero": 0,
                    "cardsOcc": 1,
                    "cardsStranded": 0,
                    "ctrlsZero": 0,
                    "ctrlsOcc": 4,
                    "ctrlsStranded": 0,
                    "starved": 0,
                    "small": 11,
                    "axeSerious": 0,
                    "axeCritical": 0,
                    "knownDebt": "`cardsOcc 1` is the MTGO PILE overlap inside the split's pool column (`DeckColumnPile`, ADR 0101 \u00a77): two copies in one Mana-Value Column are drawn stacked, so the lower one's centre hit-tests to the one on top \u2014 by design, `cardsStranded 0`, every copy is one hover or one drag away. Measured 0 this run (no pile happened to land at this exact viewport for this pool); the ceiling is kept at 1 rather than tightened, matching the load-timing-artifact caution already applied elsewhere on this row. `ctrlsOcc 4` is the Peek Panel rail over controls outside its reserve \u2014 `Table`, `Pool`, `More` and the floating `Report a bug` button (`LimitedDraftTable` reserves the rail's 224px on its own surface via `peekPanelReserve`, but those four are not inside it; `docs/findings/2583-peek-rail-occludes-controls-outside-the-reserve.md`). SEAT STATE PINNED (issue #2677): the walk now clicks the top pack tile before every measurement (`pinDraftSelection`, `surfaces.ts`), so a Selected Card and its Peek Panel are ALWAYS mounted here \u2014 this is no longer an uncontrolled inherited state. `small11` (up from a `small7` ceiling recorded before Peek Panel's CTA row and the pool's Group/Order controls were counted): browser-verified breakdown at this viewport is `Table`/`Pool`/`More` (32px toolbar), the Booster-size slider, `Pool grouping`/`Pool ordering` comboboxes (24px, only rendered once the pool is non-empty), the Peek Panel's `Close`/`Pick`/`\u2192 Side`/`Inspect` row (32px) and `Report a bug` \u2014 all the deliberate 32px/24px `--control-h-fine` branch (`src/index.css:942,946-948`), not touch debt; WCAG 2.5.8 does not bind a mouse pointer. Do NOT lower this ceiling by shrinking desktop controls; #2659 does not touch this row."
                },
                "390x844x3": {
                    "cardsZero": 0,
                    "cardsOcc": 0,
                    "cardsStranded": 0,
                    "ctrlsZero": 0,
                    "ctrlsOcc": 0,
                    "ctrlsStranded": 0,
                    "starved": 0,
                    "small": 1,
                    "axeSerious": 0,
                    "axeCritical": 0,
                    "knownDebt": "SEAT STATE PINNED (issue #2677): the walk clicks the top pack tile before every measurement (`pinDraftSelection`, `surfaces.ts`), but the Peek Panel itself does NOT mount on a phone (issue #2588 inlines its CTA row into the snap strip instead \u2014 `limited-draft-table.tsx`'s `phoneOrientation === null ? peeked : null`), so this row is unaffected by the panel either way. `small1` (tightened from 2): REAL sub-44px tap-target debt at this touch viewport (`pointer: coarse` -> the 44px `--control-h-coarse` branch, `src/index.css:942,946-948`), not a measurement artifact \u2014 one fewer control than the prior recording, a genuine improvement, not padding. Recorded as a ceiling (issue #2658) so it cannot deepen; #2659 owns lowering it further."
                },
                "844x390x3": {
                    "cardsZero": 0,
                    "cardsOcc": 0,
                    "cardsStranded": 0,
                    "ctrlsZero": 0,
                    "ctrlsOcc": 0,
                    "ctrlsStranded": 0,
                    "starved": 0,
                    "small": 2,
                    "axeSerious": 0,
                    "axeCritical": 0,
                    "knownDebt": "`small2` at this touch viewport (`pointer: coarse` -> the 44px `--control-h-coarse` branch, `src/index.css:942,946-948`) is REAL sub-44px tap-target debt, not a measurement artifact. Recorded as a ceiling (issue #2658) so it cannot deepen; #2659 owns lowering it as the surface's controls are fixed."
                },
                "820x1180x2": {
                    "cardsZero": 1,
                    "cardsOcc": 1,
                    "cardsStranded": 0,
                    "ctrlsZero": 0,
                    "ctrlsOcc": 5,
                    "ctrlsStranded": 0,
                    "starved": 1,
                    "small": 4,
                    "axeSerious": 0,
                    "axeCritical": 0,
                    "knownDebt": "`cardsZero 1` is the Peek Panel's own thumbnail `<img>` before its Scryfall art loads (cold-cache load-timing artifact, not a layout fact); `cardsOcc 1` is the same MTGO PILE overlap documented on this surface's other split viewports. Both measured 0 this run and are kept at their prior ceiling of 1 rather than tightened. SEAT STATE PINNED (issue #2677): the walk clicks the top pack tile before every measurement (`pinDraftSelection`, `surfaces.ts`), so a Selected Card and its Peek Panel are ALWAYS mounted here. `ctrlsOcc 5` (up from 4): browser-verified breakdown is the SAME 4-item Peek Panel rail gap already documented on this row (`Table`/`Pool`/`More`/`Report a bug`, none inside `peekPanelReserve`) PLUS ONE new, unrelated occlusion \u2014 the Sideboard zone's own `View \u25be` toolbar control self-occluded by its wrapped `Sideboard 0` header (`CompactChromeDisclosure`, `src/components/deckbuilder/deck-zone-surface.tsx:587`), a tablet-portrait-only fold gap that landed with #2755/#2671 (`useIsTabletPortrait`) after this row's ceiling was last recorded \u2014 discovered incidentally while re-recording for this issue, not caused by the seat-state fix. `starved 1` is the Booster grid's own scroller inside the split's pack column (ADR 0101 \u00a76), unchanged and by design. `small4` is REAL sub-44px tap-target debt (`pointer: coarse`), unchanged; #2659 owns lowering it."
                },
                "1180x820x2": {
                    "cardsZero": 1,
                    "cardsOcc": 1,
                    "cardsStranded": 0,
                    "ctrlsZero": 0,
                    "ctrlsOcc": 4,
                    "ctrlsStranded": 0,
                    "starved": 0,
                    "small": 4,
                    "axeSerious": 0,
                    "axeCritical": 0,
                    "knownDebt": "`cardsOcc 1` is the MTGO PILE overlap inside the split's pool column (`DeckColumnPile`, ADR 0101 \u00a77): two copies in one Mana-Value Column are drawn stacked, so the lower one's centre hit-tests to the one on top. It appeared the moment this seat's Pool stopped being empty \u2014 nothing in issue #2588 touches this arm, which renders the tablet/desktop SPLIT \u2014 and it is by design: `cardsStranded 0`, every copy is one hover or one drag away. `cardsZero 1` is the Peek Panel's own thumbnail `<img>`, which measures 0x0 until its Scryfall art loads; the lane runs with no warm image cache, so every card tile on this surface renders its loading placeholder (see the screenshot). It is not a collapsed pack tile: `cards n16` = 15 pack tiles + that thumbnail, and `cardsStranded 0` at every viewport. `ctrlsOcc 4` is the Peek Panel rail over controls that sit OUTSIDE the reserve. `LimitedDraftTable` reserves the rail's 224px on its own surface (`peekPanelReserve`), but the floating bug-report button and the zoom slider's end caps are not inside it \u2014 the shape already drafted in `docs/findings/2583-peek-rail-occludes-controls-outside-the-reserve.md`, unchanged by issue #2587 or #2588 (the panel and its reserve moved route, then stopped mounting on a PHONE; this is a tablet). Recorded with a live pack AND a Selected Card, which is the only state that mounts the panel at all. `small4` at this touch viewport (`pointer: coarse` -> the 44px `--control-h-coarse` branch, `src/index.css:942,946-948`) is REAL sub-44px tap-target debt, not a measurement artifact. Recorded as a ceiling (issue #2658) so it cannot deepen; #2659 owns lowering it as the surface's controls are fixed."
                }
            }
        },
        "draft-pool-stop": {
            "label": "Draft Room, pool stop (/limited/<id>/draft, swiped)",
            "status": "budgeted",
            "viewports": {
                "1440x900x2": {
                    "cardsZero": 0,
                    "cardsOcc": 1,
                    "cardsStranded": 0,
                    "ctrlsZero": 0,
                    "ctrlsOcc": 4,
                    "ctrlsStranded": 0,
                    "starved": 0,
                    "small": 11,
                    "axeSerious": 0,
                    "axeCritical": 0,
                    "knownDebt": "`cardsOcc 1` is the MTGO PILE overlap in the split's pool column (`DeckColumnPile`, ADR 0101 \u00a77) \u2014 measured 0 this run, ceiling kept at 1 (load-timing artifact caution, same as `draft-pick`'s row). SEAT STATE PINNED (issue #2677): the walk now clicks the top pack tile before every measurement (`pinDraftSelection`, `surfaces.ts`), so this surface is measured in the SAME Selected-Card state as `draft-pick`, not the 'no Selected Card' state this row previously assumed \u2014 see that row's note for the itemised cause of `ctrlsOcc 4` and `small 11` (the Peek Panel rail plus its CTA row and the pool's Group/Order controls); at this viewport the split layout means `draft-pool-stop` and `draft-pick` render the identical DOM (only the pool's scroll offset differs), so the two rows measure byte-identical here by construction, not coincidence."
                },
                "390x844x3": {
                    "cardsZero": 0,
                    "cardsOcc": 6,
                    "cardsStranded": 0,
                    "ctrlsZero": 0,
                    "ctrlsOcc": 6,
                    "ctrlsStranded": 0,
                    "starved": 0,
                    "small": 6,
                    "axeSerious": 0,
                    "axeCritical": 0,
                    "knownDebt": "REGRESSION recorded, not a defect: this is the FIRST real measurement of this surface with a non-empty pool. The surface's own walk comment (`surfaces.ts`) already flags that an empty pool makes `LimitedDraftPool` render an `EmptyState` with nothing to occlude, scoring a VACUOUS `cardsOcc 0`/`ctrlsOcc 0` pass \u2014 exactly the prior ceiling. `cardsOcc 6`/`ctrlsOcc 6`: browser-verified (`elementFromPoint` on each occluded element) \u2014 the pack pane's own persistent status bar (`DraftPackStatusBar`) and the pool strip's own toggle button sit, BY DESIGN, over the tail of the pack's booster `<ul>` while parked at the pool stop (`draft-portrait-panes.tsx`: 'the pack's status/Peek bar and the pool's tab never go away, they only swap which one has the room') \u2014 the player is not meant to interact with pack tiles from the pool pane, and `cardsStranded 0` at every viewport (every covered tile is one swipe back to `pack` away). Unrelated to Selected Card: the occluders are the status bar and the Pool toggle, never the Peek Panel (which does not mount on a phone). `small6` (tightened from 9, same touch-viewport `pointer: coarse` -> 44px `--control-h-coarse` branch as every other touch row on this surface) \u2014 REAL, not an artifact; #2659 owns lowering it further."
                },
                "844x390x3": {
                    "cardsZero": 0,
                    "cardsOcc": 2,
                    "cardsStranded": 0,
                    "ctrlsZero": 0,
                    "ctrlsOcc": 2,
                    "ctrlsStranded": 0,
                    "starved": 0,
                    "small": 3,
                    "axeSerious": 0,
                    "axeCritical": 0,
                    "knownDebt": "`cardsOcc 2` / `ctrlsOcc 2` are two DIFFERENT things, both benign, both measured (2026-08-20, `elementFromPoint` on each element's visible slice). ONE is the pool's own pile overlap (two lands in one Mana-Value Column \u2014 `DeckColumnPile`, ADR 0101 \u00a77). The OTHER is a Booster tile of the parked pack: at the pool stop `draft-landscape-panes.tsx` keeps the grid mounted under `visibility: hidden` (15 remote images that would otherwise repaint from empty on every swipe), and the scroller still shows the pane's last 20%, where the collapsed pack column is painted over it. The probe skips a `visibility: hidden` element as an OCCLUDER but still counts it as occludED, so an invisible tile scores `occ` \u2014 nothing a player can see is covered, and `cardsStranded 0` at every viewport. `small9` at this touch viewport (`pointer: coarse` -> the 44px `--control-h-coarse` branch, `src/index.css:942,946-948`) is REAL sub-44px tap-target debt, not a measurement artifact. Recorded as a ceiling (issue #2658) so it cannot deepen; #2659 owns lowering it as the surface's controls are fixed. RE-MEASURED 2026-08-22 (issue #2665, the landscape-phone Column rung): `small` LOWERED 9 -> 3 at this viewport and nowhere else. The six sub-44px targets that went away are POOL CARD TILES: this surface sets no `--card-w` of its own and inherited the board's global `min(8rem, 10vw, 9.5dvh)`, which resolves to 37.0px here \u2014 every tile a `role=\"button\"` well under the 44px minimum. The rung's tile floor takes them to 112.0px, clearing it. `cardsOcc 2` / `ctrlsOcc 2` are unchanged at 2/2 and stay exactly as documented above. The other four viewports on this surface measured byte-identical to the pre-change tree."
                },
                "820x1180x2": {
                    "cardsZero": 0,
                    "cardsOcc": 1,
                    "cardsStranded": 0,
                    "ctrlsZero": 0,
                    "ctrlsOcc": 5,
                    "ctrlsStranded": 0,
                    "starved": 1,
                    "small": 4,
                    "axeSerious": 0,
                    "axeCritical": 0,
                    "knownDebt": "`cardsOcc 1` is the MTGO PILE overlap in the split's pool column (`DeckColumnPile`, ADR 0101 \u00a77) \u2014 measured 0 this run, ceiling kept at 1 (load-timing artifact caution). SEAT STATE PINNED (issue #2677): the walk clicks the top pack tile before every measurement (`pinDraftSelection`, `surfaces.ts`); at this viewport the split layout means `draft-pool-stop` renders the identical DOM as `draft-pick` (only the pool's scroll offset differs), so `ctrlsOcc 5` and `starved 1` are the SAME two things documented on `draft-pick`'s 820x1180x2 row: the 4-item Peek Panel rail gap plus the Sideboard zone's `View \u25be` self-occlusion (`deck-zone-surface.tsx:587`, `useIsTabletPortrait`/#2755, unrelated to this issue's fix), and the Booster grid's own scroller (by design). `small4` is the same REAL sub-44px tap-target debt as every touch row on this surface."
                },
                "1180x820x2": {
                    "cardsZero": 0,
                    "cardsOcc": 1,
                    "cardsStranded": 0,
                    "ctrlsZero": 0,
                    "ctrlsOcc": 4,
                    "ctrlsStranded": 0,
                    "starved": 0,
                    "small": 4,
                    "axeSerious": 0,
                    "axeCritical": 0,
                    "knownDebt": "`cardsOcc 1` is the MTGO PILE overlap in the split's pool column (`DeckColumnPile`, ADR 0101 \u00a77) \u2014 measured 0 this run, ceiling kept at 1 (load-timing artifact caution, same as `draft-pick`'s row). SEAT STATE PINNED (issue #2677): the walk clicks the top pack tile before every measurement (`pinDraftSelection`, `surfaces.ts`); at this viewport the split layout means `draft-pool-stop` and `draft-pick` render the identical DOM (only the pool's scroll offset differs), so `ctrlsOcc 4` is the SAME 4-item Peek Panel rail gap documented on `draft-pick`'s 1180x820x2 row (`Table`/`Pool`/`More`/`Report a bug`), not the 'no Selected Card' state this row previously assumed."
                }
            }
        },
        "deck-detail": {
            "label": "Deck detail (/decks/mono-red-burn)",
            "status": "budgeted",
            "viewports": {
                "1440x900x2": {
                    "cardsZero": 0,
                    "cardsOcc": 35,
                    "cardsStranded": 0,
                    "ctrlsZero": 0,
                    "ctrlsOcc": 0,
                    "ctrlsStranded": 0,
                    "starved": 0,
                    "small": 12,
                    "axeSerious": 0,
                    "axeCritical": 0,
                    "knownDebt": "RE-RECORDED (issue #2591 review round 2, PR #2647): the row now walks the code-defined preset `mono-red-burn` (40 cards) instead of a deployment-only `the-deck` slug (H1 \u2014 reproducibility), so the counts here are lower than the prior round's (54->35 etc). cardsOcc 35/40 is the deck's own Mana Pile View (`mana-pile.tsx`), pre-existing and unchanged by issue #2591. Each pile fans its cards `OFFSET_Y_REM` (1.4rem) apart absolutely-positioned top-down (`mana-pile.tsx:12-34`); at a `--card-h` well over 1.4rem, every card but the last N in a pile has its centre covered by the one after it, so the probe's centre-point occlusion check reads it as occluded \u2014 a DIFFERENT height formula from the deckbuilder's own `DeckColumnPile` (`pileHeight(n)`, `cardsOcc 0` on the `deck-builder` row above), which staggers just enough to keep each centre clear. Tracked as a candidate follow-up, not fixed here: docs/findings/2591-deck-detail-manapileview-occlusion.md. `small12` at 1440x900x2 (pointer: fine) is the deliberate 32px `--control-h-fine` branch (`src/index.css:942,946-948`) showing up in a POINTER-BLIND measurement, not a defect \u2014 WCAG 2.5.8 (cited in that CSS comment) is a TOUCH-target rule and does not bind a mouse pointer. Do NOT lower this ceiling by shrinking desktop controls; #2659 (the sibling fix issue) does not touch this row."
                },
                "390x844x3": {
                    "cardsZero": 0,
                    "cardsOcc": 32,
                    "cardsStranded": 0,
                    "ctrlsZero": 0,
                    "ctrlsOcc": 0,
                    "ctrlsStranded": 0,
                    "starved": 0,
                    "small": 2,
                    "axeSerious": 1,
                    "axeCritical": 0,
                    "knownDebt": "RE-RECORDED against `mono-red-burn` (see 1440x900x2's note for why). Same Mana Pile View fan-stacking, pre-existing. `starved` cleared 1->0 against this deck's card count; 1 axe serious (scrollable-region-focusable) mirrors the SAME shape already carried on `deck-builder`'s phone-portrait row. `small2` at this touch viewport (`pointer: coarse` -> the 44px `--control-h-coarse` branch, `src/index.css:942,946-948`) is REAL sub-44px tap-target debt, not a measurement artifact. Recorded as a ceiling (issue #2658) so it cannot deepen; #2659 owns lowering it as the surface's controls are fixed."
                },
                "844x390x3": {
                    "cardsZero": 0,
                    "cardsOcc": 15,
                    "cardsStranded": 0,
                    "ctrlsZero": 0,
                    "ctrlsOcc": 0,
                    "ctrlsStranded": 0,
                    "starved": 1,
                    "small": 7,
                    "axeSerious": 0,
                    "axeCritical": 0,
                    "knownDebt": "RE-RECORDED against `mono-red-burn` (see 1440x900x2's note for why) \u2014 occ15 is unchanged from the prior round at this viewport. Same Mana Pile View fan-stacking as 1440x900x2; fewer piles fit the shorter viewport so occ is lower (15) with more genuinely reachable via scroll. 1 starved container mirrors `deck-builder`'s own phone-landscape row. `small7` at this touch viewport (`pointer: coarse` -> the 44px `--control-h-coarse` branch, `src/index.css:942,946-948`) is REAL sub-44px tap-target debt, not a measurement artifact. Recorded as a ceiling (issue #2658) so it cannot deepen; #2659 owns lowering it as the surface's controls are fixed."
                },
                "820x1180x2": {
                    "cardsZero": 0,
                    "cardsOcc": 35,
                    "cardsStranded": 0,
                    "ctrlsZero": 0,
                    "ctrlsOcc": 0,
                    "ctrlsStranded": 0,
                    "starved": 0,
                    "small": 7,
                    "axeSerious": 0,
                    "axeCritical": 0,
                    "knownDebt": "RE-RECORDED against `mono-red-burn` (see 1440x900x2's note for why). Same Mana Pile View fan-stacking, pre-existing. `axeSerious` cleared 1->0 against this deck's card count. `small7` at this touch viewport (`pointer: coarse` -> the 44px `--control-h-coarse` branch, `src/index.css:942,946-948`) is REAL sub-44px tap-target debt, not a measurement artifact. Recorded as a ceiling (issue #2658) so it cannot deepen; #2659 owns lowering it as the surface's controls are fixed."
                },
                "1180x820x2": {
                    "cardsZero": 0,
                    "cardsOcc": 35,
                    "cardsStranded": 0,
                    "ctrlsZero": 0,
                    "ctrlsOcc": 0,
                    "ctrlsStranded": 0,
                    "starved": 0,
                    "small": 7,
                    "axeSerious": 0,
                    "axeCritical": 0,
                    "knownDebt": "RE-RECORDED against `mono-red-burn` (see 1440x900x2's note for why). Same Mana Pile View fan-stacking, pre-existing. `starved` cleared 1->0 against this deck's card count. `small7` at this touch viewport (`pointer: coarse` -> the 44px `--control-h-coarse` branch, `src/index.css:942,946-948`) is REAL sub-44px tap-target debt, not a measurement artifact. Recorded as a ceiling (issue #2658) so it cannot deepen; #2659 owns lowering it as the surface's controls are fixed."
                }
            }
        }
    }
}
