// PCY (Prophecy) set barrel — re-exports every colour module so the registry's
// `import * as pcy from "./sets/pcy"` resolves here unchanged (ADR 0043). Home
// set (earliest paper printing, ADR 0041) for the Vintage Cube pitch spell Foil
// (blue), issue #690 / #1003.

export * from "./blue";
