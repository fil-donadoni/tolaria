// Invasion (INV) — green cards, split by colour per ADR 0043. The registry's
// `import * as inv from "./sets/inv"` resolves through inv/index.ts. Modern
// Scryfall oracle text is authoritative (ADR 0004).
//
// No green cards implemented yet — free-tranche and capability cards land in
// later slices (parent PRD #1063). Kept empty for the consistent per-colour
// shape ("sparse modules are accepted", ADR 0043).

export {};
