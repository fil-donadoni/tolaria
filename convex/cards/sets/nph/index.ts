// NPH set barrel — re-exports every colour module so the registry's
// `import * as nph from "./sets/nph"` resolves here unchanged (ADR 0043).
// New Phyrexia (2011) — home set for the Phyrexian-mana cube cluster (#696).

export * from "./blue";
export * from "./black";
