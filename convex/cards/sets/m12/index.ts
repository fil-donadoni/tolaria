// M12 set barrel — re-exports every colour module so the registry's
// `import * as m12 from "./sets/m12"` resolves here unchanged (ADR 0043).
// Magic 2012 (2011) — home set for the Vintage Cube's clone cluster residue
// (issue #1563: Phantasmal Image, split off #1528).

export * from "./blue";
