// Limited Edition Alpha (LEA), split by colour per ADR 0043.
//
// LEA has no multicolor cards (gold cards debuted in Legends), so this module
// is intentionally empty — kept for the consistent per-colour shape (ADR 0043).

export {};
